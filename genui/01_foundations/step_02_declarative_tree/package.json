{
  "name": "genui-01-step-02-declarative-tree",
  "version": "0.1.0",
  "description": "Declarative generative UI: a catalog, a JSON Schema, a partial-JSON parser, two document shapes. No dependencies, no build.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "demo": "python demo.py"
  }
}
