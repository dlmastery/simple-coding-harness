Somewhere in this project a function named `parse_config` is defined. Use a
task subagent to find the file that defines it; do not search yourself. Then
answer with just the file name, for example `something.py`.
