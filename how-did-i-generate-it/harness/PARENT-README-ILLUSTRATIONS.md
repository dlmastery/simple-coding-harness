# Illustrations in the original parent tutorial

The user requested the existing harness illustrations inside the original parent
README on `main`, not only in the rewritten course entrance on the rebuild branch.

Base: `21bf59a8cc53d3e67d3a6ae3675a4fc7489a85aa`.
Image source: `4764b9c9b1a021b4094e9fe3008544b6a753ac5c`.

The [original README backup](backups/README-before-illustrations.md) retains the
pre-edit file. Its relative links belong to its original repository-root location.
The [historical version](https://github.com/dlmastery/simple-coding-harness/blob/21bf59a8cc53d3e67d3a6ae3675a4fc7489a85aa/README.md)
keeps those links in their original context.

The original tutorial text and code examples remain. Eight selected figures are
embedded: the overview near the opening, then foundations, controls, adapters,
tools, recovery, production and server figures at stages 1 and 9 and steps 16,
21, 31, 39 and 46. Each has a short explanation and a full-size link. The opening
links the [visual guide](../../harness/VISUAL-GUIDE.md), whose lesson links now
point into the original tutorial. No lesson implementation is changed.

Existing images are reused without new generation. All eleven original image
versions, prompts, review notes and hashes are retained in the repository. Only
the eight reviewed selections appear in the tutorial and visual guide. See the
[visual provenance](visuals/README.md).

The user's follow-up requests a prominent link to the newer course README.
A notice directly below the original title links to the rebuilt parent README
on `codex/rsi-masterclass-rebuild` and identifies that branch explicitly. It also
links to the local harness visual guide. The original tutorial stays on this page.

Publication checks: 74 local links and referenced tutorial anchors resolve;
all eleven image files match their source Git blobs; the parent README diff
contains additions only; the backup's Git-normalized content matches the base
README. Whitespace checks pass. No runtime tests are needed for this illustration
placement change. An initial redundant hash probe combined incompatible Git
options; the separate backup-to-base hash comparison completed successfully.
