# Server figure correction

Edit the existing server infographic, preserving its excellent white-background illustration, typography, client and service. Correct the outcome arrows: remove ALL current arrows between the event-stream row and the two outcome boxes. Draw exactly one green arrow FROM the Terminal status card TO the Terminal event received box. Draw exactly one amber dashed arrow FROM the gap BETWEEN Tool result and Terminal status TO Connection lost; label this dashed arrow No terminal event. There must be no arrow from a tool result to Terminal event received, and no arrow from Terminal status to Connection lost. Replace Turn is complete. Inspect the status. with Turn ended. Inspect the status. Replace the bottom sentence Verifies whether the turn reached a valid terminal state. with Tests completion, failure and interruption handling. Preserve Done, Cancelled and Error as distinct results. Do not introduce new arrows or text.
