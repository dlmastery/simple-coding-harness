const L=(id,slug,title,body)=>({id,theme:id.slice(0,2),slug,title,...body});
export const structure = [
L('03.01','dependencies','Draw the dependencies',{
 build:'An execution graph for the process you already ran.', why:'A numbered list hides why one action must precede another. Dependencies explain that order.', start:'The fixed process, its trace, and a fresh diagram workspace.', budget:'No model fits. One graph validation and one deliberately invalid ordering.',
 how:'Draw each action as a node. An arrow from inspect data to validate split means the latter needs the former’s output. A directed acyclic graph, or DAG, has directed arrows and no cycle. A loop is a cycle, so a workflow containing a retry is not a DAG unless the retry is represented as a separate bounded operation.',
 predict:'Can the metric checker run before predictions exist?',
 steps:[['Name the dependencies','Explain order through required inputs.','Create WORKFLOW.md with nodes frame, inspect, split, fit, check, report. For each edge, name the artifact it carries. Render a simple diagram and retain the editable source.','Every arrow has a reason.'],['Validate an ordering','Turn the graph into a small executable check.','Generate a local graph-order checker. Verify the normal sequence, then try an order that checks predictions before fitting. Keep the failure and explanation.','The invalid order fails because a required artifact is unavailable.']],
 check:'The graph has explicit edges and a valid order. The checker rejects the invalid order. The diagram labels artifacts rather than implying unexplained communication.', change:'Remove the inspect-to-split edge in a copy. Explain what important information the split designer could now miss.', takeaways:['Dependencies describe what an action needs.','A valid order respects every prerequisite.','A diagram becomes more useful when its edges name artifacts.'],
 quiz:[['What does an arrow mean here?','The destination needs a declared result from the source; it is not merely a visual sequence.'],['Can two nodes appear in either order?','Yes, if neither depends on the other and their side effects do not conflict.'],['Is a graph with a retry edge always a DAG?','No. A retry can create a directed cycle.'],['What does the invalid-order test establish?','That the checker enforces one actual dependency, not that every possible workflow is correct.']], next:'Dependencies give an order. Branches let different conditions choose different routes.'}),
L('03.02','branch','Route different failures differently',{
 build:'A branch that sends invalid data to repair and valid data to modeling.', why:'Trying another model cannot fix a missing target or a leaked feature. The response should match the failure.', start:'The data report and dependency graph. Make teaching copies; preserve pinned data.', budget:'No fits required. Test two data fixtures and one ambiguous case.',
 how:'A branch is a decision with explicit conditions. If required fields are absent, stop before fitting. If data checks pass, proceed. If the evidence is incomplete, report uncertainty. Do not route an unknown condition into the success branch by default.',
 predict:'What should happen when a data check has no result at all?',
 steps:[['Define the branch','Make each route meaningful.','Generate a small routing tool with states valid, invalid, and unknown. Use a valid sample, a copy missing cnt, and a missing check report. Write the route conditions in WORKFLOW.md.','Unknown has its own stop or review route.'],['Run all routes','Test the conditions instead of only the happy path.','Execute each fixture through the router. Save ROUTES.md with input, condition, chosen action, and exit status. Confirm that invalid and unknown inputs cannot reach fitting.','The trace explains why each route was taken.']],
 check:'All three states are exercised. No invalid or unknown input reaches the fit action. Fixture mutations stay outside source data.', change:'Change “unknown means stop” to “unknown means pass” in a labelled copy. Show which faulty input is now accepted.', takeaways:['Different failures need different actions.','Missing evidence is not evidence of validity.','Branch conditions should be visible in the trace.'],
 quiz:[['Why should a missing target stop fitting?','The prediction problem cannot be trained or evaluated as declared.'],['Is unknown the same as invalid?','No. One lacks evidence; the other has evidence of a violation. Both may require stopping.'],['Why test every route?','A successful valid case does not show that invalid inputs are blocked.'],['Would another model solve the routing problem?','No. The problem concerns input validity and control flow, not model capacity.']], next:'Some checks can proceed independently. Learn how to join their results.'}),
L('03.03','join','Join independent checks',{
 build:'A join that waits for both a data-quality check and a resource check.', why:'One successful check must not conceal another missing or failed check.', start:'The branch router and a candidate recipe. Use small local check fixtures.', budget:'No model fits; three join scenarios.',
 how:'Two checks can inspect different properties of the same candidate. Their results must carry its identity and contract version. A join waits for both, verifies identity, then applies a declared rule. Running these checks sequentially can test the logic; actual parallel execution adds timing and coordination concerns.',
 predict:'Should a data pass for candidate A combine with a budget pass for candidate B?',
 steps:[['Define the join','State what must agree.','Generate separate data and resource check functions. Their results must identify candidate and contract. Add a join that requires both passes for the same candidate. Do not launch agent subworkers.','The dependency structure is explicit without needing multiple agents.'],['Exercise mismatches','Test missing and stale results.','Run three cases: both checks pass for A; A has only one result; data passes for A while resources pass for B. Retain JOIN-REPORT.md and all check records.','Only the complete matching case proceeds.']],
 check:'The join rejects mismatched identities and incomplete evidence. The report states whether checks ran sequentially or concurrently.', change:'Delay one check result in a local simulation. Explain why the other result alone cannot authorize the next action.', takeaways:['Independent work still needs coordinated completion.','Results must refer to the same candidate and contract.','A graph supports parallelism; it does not prove parallel execution occurred.'],
 quiz:[['Why include candidate identity?','Otherwise valid results from different experiments can be combined incorrectly.'],['Does a join mean both checks must start simultaneously?','No. It specifies the conditions for proceeding after their results exist.'],['What should a missing check cause?','Waiting within a limit or stopping with an incomplete state, according to the declared rule.'],['What extra risk does concurrency add?','Races, stale results, conflicting writes, and harder accounting; the logic must remain explicit.']], next:'Place a bounded retry inside this graph without losing its stop rules.'}),
L('03.04','cycle','Put a bounded retry inside the graph',{
 build:'A graph with one explicit repair cycle and a terminal failure path.', why:'A return arrow can hide unlimited work unless the retry state travels with it.', start:'The router and join. A teaching report with one correctable missing field.', budget:'Two repair attempts maximum; no model fits.',
 how:'A cycle returns to an earlier action. Its state must include attempts used and the last failure. A repair can change the failing artifact, but it cannot redefine the required fields. The graph exits on success or when the original budget is exhausted.',
 predict:'If a repair does not change the missing field, what should the second failure cause?',
 steps:[['Add the cycle','Preserve state across the return edge.','Extend the local graph with check, repair, and recheck nodes. Permit two repairs. Keep the original validation rule fixed. Save the graph and state transitions.','The cycle carries attempt count and failure reason.'],['Test success and exhaustion','Check both exit paths.','Run a fixture whose first repair restores the field, then a fixture whose repair leaves it missing. Keep both traces. Verify that the second run ends at the budget, without claiming success.','A stopped failure is a valid terminal outcome.']],
 check:'Both traces end. Attempts are monotonic and bounded. The verifier rule is unchanged between checks.', change:'Remove the failure feedback from the repair input. Predict how that could waste attempts even with a valid stop rule.', takeaways:['Cycles need explicit state and exit conditions.','Stopping with failure is better evidence than pretending a repair worked.','A repair changes an artifact, not the definition of validity.'],
 quiz:[['Why is this graph not a DAG?','Its recheck path returns to an earlier node, forming a directed cycle.'],['Can a bounded loop still be unhelpful?','Yes. It can repeat ineffective actions while respecting its budget.'],['What stays fixed during repair?','The task and validation requirements used to judge the repair.'],['What is the expected outcome after two ineffective repairs?','A recorded terminal failure with the spent attempts, not another hidden retry.']], next:'Now recover a failed branch without repeating unrelated work.'}),
L('03.05','recover','Resume only the affected work',{
 build:'A recovery trace that preserves valid upstream artifacts and reruns affected descendants.', why:'Restarting everything wastes work. Reusing everything risks stale results. Dependencies tell you what must change.', start:'The graph with data check, fit, metric check, and report. Use the existing baseline predictions.', budget:'No new fit unless an upstream data or recipe change actually requires it; at most one fit.',
 how:'If report formatting fails, valid predictions need not be recomputed. If the dataset changes, downstream transforms, model results, and reports may be stale. Recovery follows the dependency graph and artifact versions. A successful file is reusable only when its inputs still match.',
 predict:'Which nodes should rerun after a report-writing failure? What if the split changed instead?',
 steps:[['Inject a reporting failure','Keep upstream computation valid.','Copy the baseline artifacts into a diagnostic workspace. Make the report node fail intentionally while preserving predictions and checks. Record the failure and dependencies.','The model output remains available despite the failed report.'],['Recover selectively','Use dependencies to decide what to reuse.','Repair only the report node and resume. Show that no new fit ran. Then simulate a changed split version and list the descendants that would need invalidation, without mixing their old scores.','The recovery plan differs for a downstream formatting error and an upstream scientific change.']],
 check:'The successful recovery does not create an extra fit. The changed-split case invalidates all dependent evidence.', change:'Try reusing a metric report after substituting a prediction file. Require the version check to detect the stale dependency.', takeaways:['Recovery should follow dependency and version information.','A downstream failure does not always invalidate upstream work.','Changed scientific inputs can invalidate an entire comparison.'],
 quiz:[['Why can report repair skip training?','The model’s inputs and predictions remain unchanged and validated.'],['Why does a new split require more work?','It changes training and evaluation membership, so earlier model results no longer answer the same comparison.'],['Is file existence enough for reuse?','No. Its input versions and completion state must match.'],['What evidence shows selective recovery worked?','The trace records reused artifacts and the absence of an extra fit, with the repaired report produced.']], next:'Separate the planned graph from the data flow and actual trace.'}),
L('03.06','three_views','Read the plan, data flow, and trace',{
 build:'Three views of one run: allowed actions, artifact movement, and actual events.', why:'A diagram can look correct even when execution took a different route.', start:'The workflow graph, a successful trace, and a failed trace from this theme.', budget:'No fits; inspect two existing traces.',
 how:'The control graph says which action may follow. Data flow says which artifact each action consumes or produces. The trace records what happened at a particular time. A branch can exist in the graph without being taken in a run. A missing trace event is not supplied by drawing the node.',
 predict:'Does a drawn verifier node prove that the verifier ran?',
 steps:[['Separate the views','Make their questions distinct.','Create VIEWS.md with a control graph, a data-flow table, and a time-ordered trace for one completed run. Use actual artifact names and candidate IDs.','Each view answers a different question.'],['Find the mismatch','Use a failed run to test the distinction.','Compare the failed trace with the intended graph. Identify an allowed-but-unexecuted action and an output that therefore cannot be claimed. Save the audit.','The audit refuses to treat a planned check as completed evidence.']],
 check:'VIEWS.md distinguishes permission to act, required data, and observed execution. Every claimed completed check has a trace event and output.', change:'Add a new check node to the plan without rerunning the experiment. Explain why old results do not gain that check retroactively.', takeaways:['Plans specify possibilities; traces record events.','Data flow exposes input dependencies.','A later diagram edit cannot change historical evidence.'],
 quiz:[['What does the control graph establish?','The intended allowed structure of execution.'],['What does the trace establish?','Recorded events for a particular run, subject to the trace’s integrity and completeness.'],['Why is data flow separate?','Actions can occur in order yet consume the wrong or stale artifacts.'],['Can a planned check be counted as passed?','No. It must execute against the relevant candidate and produce valid evidence.']], next:'The routes are explicit. Now make the meaning of their objects explicit too.'}),
L('04.01','entities','Name the objects in an experiment',{
 build:'A small vocabulary for data, columns, targets, partitions, models, metrics, and evidence.', why:'“Improve the model” is ambiguous if one speaker means fitted parameters and another means the whole agent system.', start:'One baseline report, its task brief, and prediction file.', budget:'No fits. Classify ten objects from the existing run.',
 how:'An entity is an object you need to distinguish. A type states what kind of object it is. The bike table is a dataset; cnt is a column playing the target role; MAE is a metric; 159.948 is a measured value for a particular candidate and partition. Keeping these separate prevents category mistakes.',
 predict:'Are “MAE” and “159.948 rentals per hour” the same kind of thing?',
 steps:[['Build the vocabulary','Ground each term in an actual artifact.','Use review-domain. Create VOCABULARY.md with object, type, plain-language definition, and example from the baseline. Include dataset, column, target role, partition, fitted model, recipe, metric, and measurement.','The vocabulary distinguishes a rule from its result.'],['Classify a confusing case','Test whether definitions help.','Classify the phrase “the model improved” in three cases: lower selection error, edited research skill, and changed language-model weights. Rewrite each claim precisely.','Different mutable objects receive different names.']],
 check:'Definitions include concrete examples and do not equate a recipe with a fitted model or a metric with its measured value.', change:'Replace every use of “score” with its precise metric, candidate, partition, and unit. Notice which missing context becomes visible.', takeaways:['Shared names reduce ambiguous instructions.','A role can differ from an object’s type.','A measured value needs context to be meaningful.'],
 quiz:[['Is cnt inherently the target in every possible task?','No. It is a column assigned the target role in this task.'],['Is a metric the same as a measurement?','No. MAE is a calculation rule; its value comes from specific predictions and actual outcomes.'],['What differs between a recipe and a fitted model?','The recipe specifies how to fit; the fitted model contains parameters learned from particular training data.'],['Does a vocabulary alone enforce correctness?','No. Relations and executable checks are needed to test important claims.']], next:'Connect these objects with relations that express the experiment’s meaning.'}),
L('04.02','relations','Connect data, models, and evidence',{
 build:'A readable relation table describing one ML experiment.', why:'A list of names does not tell you which model used which data or which score supports which decision.', start:'The vocabulary and baseline artifacts. Use a fresh Markdown facts file.', budget:'No fits; inspect one correct relation table.',
 how:'A relation connects two objects with a named meaning: scaler “fit on” train; search “selects on” selection; model “measured by” MAE. These facts describe the domain. An execution edge, by contrast, says which action depends on another. Both may be graphs, but they answer different questions.',
 predict:'Does “model measured by MAE” tell you which action runs next?',
 steps:[['Write the facts','Turn relationships into complete sentences.','Create DOMAIN.md as a Subject, Relation, Object Markdown table. Include model uses feature hr; scaler fit on train; search selects on selection; model measured by MAE; model predicts cnt. Explain each row in plain language.','The table describes meaning rather than execution order.'],['Check the declared relations','Run the small supplied semantic checker.','Run audit-domain on DOMAIN.md and save DOMAIN-CHECK.md. Explain its allowed relations and the limits of its three invariants.','A pass means only that the supplied rules found no violation.']],
 check:'Each relation is understandable as a sentence. The checker runs and the report states its limited coverage.', change:'Draw the execution graph beside the relation table. Identify one node name that appears in both but has a different role.', takeaways:['Relations connect otherwise isolated objects.','A domain graph and a control graph serve different purposes.','A valid table can still omit a scientifically important fact.'],
 quiz:[['What does an ontology add beyond a taxonomy?','It can specify relations and constraints, not just categories or parent-child groupings.'],['Is this a database schema?','No. A schema describes storage structure; this table expresses selected domain meanings, though the two can inform each other.'],['What is a knowledge graph here?','A set of concrete entities and relation facts using the vocabulary.'],['Does a passing check prove the whole experiment sound?','No. It covers only the declared invariants and available facts.']], next:'Add rules that distinguish plausible-looking facts from valid ones.'}),
L('04.03','invariants','State rules that must always hold',{
 build:'Three domain invariants, each with a passing and failing example.', why:'Correct spelling and valid file structure do not prevent target leakage or misuse of final data.', start:'DOMAIN.md and the bike task contract.', budget:'No fits; six small checker cases.',
 how:'An invariant is a condition that must hold for every accepted record in this task. A transform fits on training data only. Search uses selection data, not final data. A prediction input must not be derived from the target. These are meaning rules; a syntactically valid table can violate them.',
 predict:'Can a perfectly formatted table describe an invalid experiment?',
 steps:[['Pair rules with examples','Make each rule testable.','Write RULES.md with the three invariants. For each, generate a passing fact table and one failing table. Keep the intended violation explicit.','Each example isolates one rule.'],['Execute the examples','Verify the rules have consequences.','Use audit-domain on all six tables. Record expected and actual verdicts in RULE-TESTS.md. Keep every input and output.','Three valid cases pass and three targeted violations fail.']],
 check:'Every invariant has a demonstrated negative case. Unknown relations are not silently accepted.', change:'Add a new rule that MAE measurements must include units. Ask the agent to implement and test it in a workspace extension, without pretending the original tool already checked it.', takeaways:['Meaning rules catch errors that formatting checks miss.','A rule needs examples that distinguish valid from invalid cases.','Validator coverage is explicit and can grow.'],
 quiz:[['Why is a failing example useful?','It demonstrates that the checker detects the violation the rule describes.'],['Is fitting a scaler on all rows harmless because it ignores labels?','No. It uses evaluation-distribution information and violates the declared train-only procedure.'],['Can a rule be universal across every task?','Some principles generalize, but exact requirements depend on the task’s information setting and protocol.'],['What should happen with an undefined relation?','Define its meaning and tests before relying on it; do not silently treat it as valid.']], next:'Use these rules to diagnose a deliberately inconsistent experiment record.'}),
L('04.04','catch_contradictions','Catch a plausible but invalid experiment',{
 build:'A semantic failure report and a corrected relation table.', why:'A realistic mistake combines several reasonable-looking facts. The rules must connect them.', start:'The supplied domain skill, original correct facts, and a separate teaching copy.', budget:'Two domain checks, no model fits.',
 how:'Suppose a model uses a feature called total_users. Another fact says total_users is derived from the target. Neither line alone states “invalid.” Together they violate the task’s input rule. A semantic check reasons over the relation between facts.',
 predict:'Will renaming a leaked feature make it a valid input?',
 steps:[['Create the contradiction','Make the error about meaning, not syntax.','In a teaching DOMAIN.md, add model uses feature total_users and total_users derived from target. Also set scaler fit on final and search selects on final. Keep the table well formed. Run audit-domain.','The report identifies three semantic violations.'],['Repair the record','Correct facts without erasing the failure.','Save a corrected table that removes the leaked input, fits the scaler on train, and selects on selection. Recheck it. Explain which actual implementation actions would also need correction if this were a real run.','Editing a record alone cannot repair an already leaked experiment.']],
 check:'The original table fails with three concrete reasons. The corrected copy passes the supplied rules. The report distinguishes correcting documentation from rerunning invalid computation.', change:'Rename total_users to harmless_feature. Confirm that the derivation relation still triggers rejection.', takeaways:['Renaming an object does not change its meaning.','Relations can reveal a contradiction across separate facts.','Fixing a report cannot retroactively repair invalid training.'],
 quiz:[['Why is this an ontology exercise?','It uses types, relations, and invariants to check the meaning of an experiment record.'],['Why should the renamed feature still fail?','Its derivation from the target remains the relevant fact.'],['Does the corrected table validate old leaked scores?','No. The affected experiments must be rerun under the corrected protocol.'],['What remains outside the checker?','Unrecorded derivations and other scientific errors. The checker depends on adequate facts and declared rules.']], next:'Domain vocabulary changes too. Trace the consequences of a revised meaning.'}),
L('04.05','evolve_vocabulary','Change a definition without losing its consequences',{
 build:'A versioned vocabulary change and an impact report for a new forecasting task.', why:'A harmless-looking word change can alter which inputs and results are valid.', start:'The retrospective bike task, vocabulary, facts, and dependency graph.', budget:'No fits; one impact analysis and one rule extension test.',
 how:'Changing “observed weather” to “weather forecast available one day earlier” changes the information contract. Existing features may no longer satisfy it. Trace from the changed concept through feature rules, data sources, candidate recipes, and evidence. Keep the old task valid on its own terms; create a new task version.',
 predict:'Which previous scores can be reused as evidence for the new day-ahead forecasting task?',
 steps:[['Version the definition','State the changed scientific question.','Create vocabulary version 2 for a day-ahead demand task. Define forecast origin and input availability. Preserve version 1. Write the change and its reason in CHANGE.md.','The new task is distinguishable from the old one.'],['Trace the impact','Connect meaning changes to work that must be redone.','Use the relation table and execution graph to list affected data sources, features, split rules, recipes, and reports. Generate a small availability check that rejects an observed future weather input. Save its failing test.','The impact reaches more than a renamed column.']],
 check:'IMPACT.md identifies invalid evidence reuse and missing data. The availability rule has a demonstrated rejection. No unrun forecasting score is invented.', change:'Apply the same reasoning to wine quality: replace the binary threshold with ordinal prediction. Identify which metrics and model choices must be reconsidered.', takeaways:['Definitions are versioned parts of an experiment.','A semantic change can propagate into execution and evaluation.','Old results remain evidence for their original task, not automatically for a new one.'],
 quiz:[['Can the old MAE be presented as the new forecast’s result?','No. Its input setting differs from the new prediction question.'],['Why preserve the old vocabulary?','It explains historical records and prevents retroactive reinterpretation.'],['How do domain and execution graphs cooperate here?','Relations identify affected meanings; dependencies identify work that consumes those changed objects.'],['Does a binary-to-ordinal task change require only a new label name?','No. The target representation, loss, metric, model, and interpretation may all change.']], next:'Combine fixed tools, domain rules, and workflow control into a coordinated system.'}),
];
