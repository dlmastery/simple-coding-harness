// Focused worked examples at transitions where a concrete case aids intuition.
export const examples={
'00.03':`For a tiny invented training set of 10, 20, and 90 rentals, the median is 20. Predicting 20 gives absolute errors 10, 0, and 70: a total of 80. Predicting the mean, 40, gives 30, 20, and 50: a total of 100. The median wins for absolute error. This explains the baseline choice; it does not guarantee a good error on later hours.`,
'02.03':`In the saved author walkthrough, the linear model with calendar fields has selection MAE 109.81. Adding weather to that same model reduces it to 99.18. Changing the model to a tree while keeping calendar fields instead gives 125.05. One proposal helps and another hurts. Keeping the interventions separate tells you which change produced each result. Your own run must supply its own measurements.`,
'03.03':`Imagine the data check passes for candidate A, while the prediction check passes for candidate B. You have two passes, but no candidate has passed both checks. The join must ask “two passes for which candidate?” This is why identity belongs in the evidence, not just in a filename chosen by the agent.`,
'04.01':`A candidate is a recipe, such as “linear model with calendar fields.” A run is an execution of that recipe on particular data with a particular seed. Two runs can use the same candidate and still have different timings or outcomes. Keeping these objects separate lets you repeat a recipe without rewriting its history.`,
'04.03':`Consider the sentence “the scaler was fit on all rows.” Its format is perfectly clear. Its meaning violates the experiment: selection and final rows helped determine the transformation. A schema can accept the sentence while a domain rule rejects the relationship. That is the extra job performed by the ontology check.`,
'05.01':`A capable language model might still accept the tempting input casual + registered. A domain checker can reject it before any fit. The whole system avoids a mistake even though the language-model weights and the rule itself stayed fixed. The improvement comes from how the components are arranged.`,
'06.02':`Think of a builder that reads “binary classification” and produces a classifier, suitable splits, and a balanced-accuracy report. The same unchanged builder reads “hourly count regression” and produces a different harness. Its outputs differ because its inputs differ. To claim that the builder improved, you must change and evaluate the builder itself.`,
'07.03':`An agent can save “always use a tree” after one lucky result and follow that note tomorrow. Persistence and later use are visible, but the lesson may be wrong. This is retained adaptation without demonstrated improvement. The next experiment must test its scope instead of treating memory as success.`,
'08.04':`Suppose the original procedure gains nothing from memory, but a revised procedure knows how to retrieve the right note. Memory may help only when combined with that revision. Comparing just “old system” with “everything changed” hides this interaction. The four conditions separate the two changes and their combination.`,
'09.01':`Changing a tree depth changes the ML candidate. Adding “inspect error by hour” changes the research skill that proposes candidates. Adding “recompute every reported score before promoting a research-skill edit” changes the improver. The edit text can look small at all three levels. What matters is which later decisions it governs.`,
'09.04':`In the saved author-guided example, improver v1 requires recomputing the candidate score. A later round reads that version and rejects a report that claims MAE 9.0 while its predictions imply 159.95. The trace shows that the new instruction ran. It does not show that the system invented the instruction autonomously or that v1 is generally better across tasks.`,
'10.24':`For the invented reward group 0, 1, 1, 0, the mean is 0.5 and the population standard deviation is 0.5. Centering and dividing by that spread gives advantages -1, 1, 1, -1. If every reward is 1, the spread is zero: the group provides no relative ranking. The toy must handle that case explicitly. A real GRPO implementation also needs its actual objective, rollout policy, reference treatment, and training machinery.`,
'10.35':`Suppose a diagnosis says the current harness omits a needed tool call. A harness edit repairs that omission. Reusing the old diagnosis to choose the next operator can now be wrong: it describes the old state. Versioned evidence tells the scheduler when it must inspect the changed system again.`,
};
