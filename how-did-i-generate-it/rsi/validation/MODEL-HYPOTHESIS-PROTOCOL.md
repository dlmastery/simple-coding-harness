# Diagnose before the model comparison

Declared 22 September 2026. Preserve the historical lab 02.01 run and its missing pre-fit diagnosis. This is a new author walkthrough with exactly two fits in a fresh sibling workspace, not a reconstruction of the old chronology.

Read only the saved baseline selection error-by-hour table from the completed fixed-process walkthrough. Save that input, a hypothesis, an alternative explanation, a possible disconfirming result and fixed recipe fields before running either candidate. Existing public results and the author's prior exposure remain explicit. The exercise tests a documented workflow, not blind discovery.

Allocation: constant/calendar and linear/calendar, bike task, seed 17, one shared two-attempt experiment. The driver reads the saved recipe fields and checks the hypothesis hash before each fit. Both use the same task, source, split and metric; model family is the intentional change. Run the separate prediction checker twice and the comparison once. Use a 60-second timeout per command. No final evaluation, new final-outcome summary, extra fit or reopened experiment.

After execution, compare all 24 hourly selection slices, identify a remaining weak slice and a regression, and check that row-weighted hourly means match ledger MAE. Preserve hypotheses that fail; do not change the selection criterion. Record commands, timing, environment, source hashes and unknown provider cost. Inspect the existing conceptual figure and the measured chart without regenerating the conceptual art.

Archive raw artifacts with a hash manifest, link the lesson to this evidence and leave the older missing note visible. Student predictions, quizzes, fresh-agent behavior and generalization beyond the selection period remain untested.
