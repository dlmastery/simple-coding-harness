# What the completed comparison changes

The preceding turn made progress and verified a live search wait. This turn
completed search and final scoring, recomputed results, diagnosed composed
operator behavior and integrated the evidence. No interrupted attempt was
restarted and no final outcome changed a choice.

## Verified state

- Search session 45740 ended with exit 0: 288 successful attempts.
- Search audit passed 9,485 checks before any final scoring.
- Scoring session 92850 ended with exit 0: 36 successful frozen-choice refits.
- Final audit passed 10,353 checks; all original selection scores reproduced.
- Four prespecified paired contrasts, complete native scores and costs are reported.
- A no-fit constructor audit found two cross-procedure source changes with
  identical estimator settings; zero within-procedure constructor repeats.
- The archive copied and byte-verified 4,938 original files. Session 90498
  ended with exit 0. The source freeze and choice freeze remain unchanged.
- The measured chart was inspected twice. Version one is retained; version
  two names tasks plainly and uses six decimals for small naval errors.

See the [report and trace](../../../rsi/evidence/2026-09-22/tabular-comparison/README.md).
The 10,353 checks include the search checks, so these counts must not be added
as if they were disjoint tests. Constructor inspections perform zero fits.

Publication checks pass: 26 maintainer tests; 101 lessons and 5,982 local
links; 335 selected illustration placements; 101 teaching-support entries;
2,019 provenance/skill links. Student snippet discovery reports zero executable
snippets, consistent with the natural-language interface. No new broad test
coverage is inferred from that zero.

The first staging pass exposed the existing `rsi/.gitignore` rule for `runs/`:
it omitted archived run subdirectories. Only this frozen evidence directory
was explicitly staged with `git add -f`. The
[index-byte audit](TABULAR-ARCHIVE-INDEX-CHECK.csv) verifies all 4,938 original
files against their staged Git blobs, with the manifest also tracked. A local
archive count alone would not have proved that those files reached GitHub.

## What is now supported

The revised updater actually determines two later task-skill rounds. Source
inheritance and changed decisions are inspectable. Its final quality improves
two tasks, ties three and worsens one against the original updater with the
revised harness. The mean interval includes zero. Memory beats random in this
sample but loses to the fixed portfolio overall. The revised harness has no
established mean advantage over its parent.

This closes the missing execution of the declared six-procedure study. It
does not close the user's requested effective-RSI outcome. A finished success
presentation would overstate these results. Its outline, illustrations and
notes remain available; the PPTX remains pending.

## Next repair

1. Preserve this study as completed evaluation. Its tasks may inform a later
   design, but cannot remain labelled untouched evaluation for that design.
2. Fix the observable composition weakness in a separately versioned builder.
   Preserve the template's effective settings during capacity refinement, and
   check constructed behavior rather than recipe names alone. Use no-fit
   counterexamples first; do not rewrite frozen sources or rerun these scores.
3. Address allocation quality explicitly. The diverse fixed portfolio is the
   strongest control here. A revised allocation policy must compete with it,
   include its development cost, and explain which information should help
   before seeing new evaluation outcomes. Renaming that control as RSI is not
   a repair.
4. Declare any additional development/evaluation allocation and new task
   selection from source metadata before new fits. The previous 96-fit
   development phase and 324-attempt comparison are closed. No extra trial is
   authorized by silently changing a directory or resetting their ledger.
5. Run a substantive later improvement process, then evaluate it under the
   frozen external comparison. A positive selected example, repeated role
   files or inherited hashes alone cannot finish the requested outcome.

Do not restart the research sweep or generate more infographics to avoid the
empirical work. Source-method fidelity, actual later use, strong controls and
the conditional PPTX remain part of the active goal. The final report must
remain honest if an attempted repair fails.
