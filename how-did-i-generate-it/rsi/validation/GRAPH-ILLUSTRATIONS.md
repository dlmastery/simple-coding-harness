# Graph-engineering illustrations

21 September 2026. Read all six graph lessons and their support guidance, plus existing join/repair evidence to distinguish earlier execution from current authoring. Built-in generator approved; model identity not exposed. Match the main overview's craft while choosing distinct compositions for dependencies, routes, joins, cycles, recovery, and views.

## Preflight

- 03.01: six nodes and named artifact dependencies. No fits; test valid ordering and check-before-fit refusal. The pictured chain is an explanatory subset of artifact prerequisites, not a new executed pipeline.
- 03.02: three local fixtures; valid, missing cnt, missing evidence. Unknown is not invalid but must not enter success by default. No fits. Preserve pinned source data.
- 03.03: require both passes for the same candidate and contract. Added a fourth wrong-contract fixture, previously not exercised by the lab's explicit three cases. Keep missing, wrong-candidate, and wrong-contract outcomes distinct. No subworkers or claim of concurrent execution. Symbolic resource fixtures do not measure capacity.
- 03.04: fixed required field, monotonic counter, check-before-repair and recheck-after-edit, success and failure exits. Clarified two repairs per fixture and at most four across both; expected successful/ineffective traces use one and two respectively. Historical author traces remain unmodified. No model fits.
- 03.05: downstream report-format failure reuses valid upstream evidence, changed split invalidates every dependent result. The changed-split case is planning, not a new measured fit. Original versions remain archived.
- 03.06: control permissions, artifact dependencies, and actual events are distinct views. Symbolic failed-run example must not imply an unexecuted check passed. Actual lab uses two existing traces with real IDs and paths.

Exact prompts precede calls. Inspect all endpoints and conditions, all row identities, per-fixture versus whole-lab accounting, and expected versus observed status. Preserve all output versions; at most three attempts per figure.

## Selection and corrections

- **03.01: v2.** The first output omitted Report from the invalid-order row, introducing a second reason for failure. One edit restored the sixth action so the two orders differ only by exchanging Fit and Check. Reviewed all five named dependencies and both orderings. The main chain is a selected set of prerequisites; the caption names additional fit and checker inputs.
- **03.02: v1.** Three separate expected routes, valid/invalid/unknown identities, no fitting, separate source/copy folders, and an empty actual-route ledger are correct. The caption limits validity to the declared fixture check.
- **03.03: v1.** Four expected table rows match the intended cases. Both records feed the join, which requires candidate and contract agreement. No observed resource measurements or concurrency are claimed. Revised the older technical schematic to use Resource check rather than Result check and include contract identity; inspected the v7 rendering. Historical author evidence remains unchanged. The new wrong-contract test is explicitly open in the execution inventory.
- **03.04: v1.** Verified every cycle edge, condition, counter increment, and terminal. The rule supplies the check and is never edited by repair. Two expected fixture traces consume three repairs under a maximum of four; checks do not consume repair slots. Clarified the lab's per-fixture budget rather than treating its former ambiguous wording as a total limit of two.
- **03.05: v1.** Both five-node chains have correct direction. The report-only failure preserves the first four nodes conditionally; the changed-split case invalidates all four downstream descendants. The latter is a planning exercise, not an unreported run. No new score is drawn.
- **03.06: v2.** My first prompt omitted selection inputs from the fit-and-predict operation and expected row IDs from the checker. One edit adds both to the table. This is an author-preflight correction, not a generator-invented error. Fit denotes the combined tool action; the caption keeps selection targets out of training. The illustrative trace stops after the first action and cannot supply a completed check or report.

Six selections used eight generated outputs; no figure exceeded two. Exact prompts, all generated versions, and original filenames are preserved. The separate [join schematic revision](../visuals/rendered-gallery-v7.md) retains editable Mermaid source and a PNG; it is not included in image-generator counts. Its old v2 rendering remains archived.

Current coverage: 75/101 mapped labs, 26 remaining in themes 04–08, 79 selected gallery images and 122 generated versions. No new learner runs, model fits, parallel checks, or source-paper audits took place in this illustration pass. Full verification remains deferred.

## Publication verification

Republished 101 technical schematics with source mappings, all lesson pages, the visual guide, image manifest, and required-activity inventory. The manifest verified 79 selected copies among 122 generated PNGs. The course checker inspected 101 lessons and 3,949 local links with zero publication problems. The activity inventory remains 63 related evidence mappings; this is not a completed-lab count. `git diff --check` passed. The revised join schematic was also visually inspected after rendering.
