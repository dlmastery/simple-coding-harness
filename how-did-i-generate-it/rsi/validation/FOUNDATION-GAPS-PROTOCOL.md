# Foundation activity reconciliation

Author walkthrough for 03.03, 04.03, and 04.04, 21 September 2026. Use a new sibling workspace and preserve source snapshots before execution. No learner or agent subworkers participate. Zero model fits across all three labs.

For 03.03, allocate four join scenarios: matching passes; missing resource evidence; wrong candidate; wrong contract. Generate separate data and resource checks over declared teaching inputs. The data check checks finite values; the resource check compares declared requested and allowed memory. Neither is a measurement of the computer. Results name candidate, contract, check kind, and verdict. The join permits continuation only for two passes that match the requested candidate and contract. For the missing scenario, also record the additional delay exercise: data arrives at synthetic tick 0, the join stops incomplete at tick 1, and a resource result arrives at tick 2 and cannot reopen that stopped decision. This uses the same scenario, no fifth join decision. Execution is sequential; ticks are simulated.

For 04.03, allocate six unchanged audit-domain subprocesses: an allowed feature versus a feature explicitly derived from target; scaler fit on train versus final; search selecting on selection versus final. Each failing table isolates one invariant. Retain every table and output. Then implement a separate workspace MAE-units check and allocate two subprocesses: one record with units, one without. This extension requires a nonempty unit string; it does not validate dimensional correctness. Do not modify the supplied tool or claim that it enforces the extension.

For 04.04, allocate three audit-domain subprocesses: a well-formed table containing all three violations; a corrected copy; and a consistently renamed copy of the original failure. Expected failure counts are three, zero, and three. Preserve the original. Explain that fixing records cannot repair a real leaked training run.

Total: four join decisions, nine base-tool subprocesses, two extension subprocesses, zero fits. Freeze the base-tool hash and all inputs before their checks. Stop on unexpected exit, unexpected verdict, timeout, or changed frozen input/tool. Keep stdout, stderr, exit codes, measured subprocess wall time, and a source manifest. No extra reruns. Inference costs and learner understanding remain unmeasured. These fact checks cannot discover omitted derivations or verify the table against an actual experiment.
