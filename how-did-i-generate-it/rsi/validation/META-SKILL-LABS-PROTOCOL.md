# Observe a task repair, then revise its updater

Declared before execution on 21 September 2026. Use a new sibling workspace, rsi-work-2026-09-21-meta-skills. The task is a small decision inside an ML workflow: among candidates with equal selection MAE, choose the shorter measured runtime after unit conversion. The candidate rows and runtime values are constructed fixtures, not new ML measurements. Zero model fits in this walkthrough.

The coding agent reads the active Markdown updater, diagnoses an observed failure, and writes a task-skill proposal. The fixed driver reads each proposed task skill and executes its decision. It also reads the updater's check policy to choose the two internal checks. This demonstrates instruction use within one author context, not isolated agents or general automatic program synthesis.

## Preparation and 10.16

First capture two parent decisions under S0: an all-seconds regression case and a milliseconds-versus-seconds failure case. These are two separately allocated prerequisite checks. S0 knows only seconds and incorrectly treats unknown units as seconds.

Freeze META-SKILL-v0 before editing. Under its diagnosis, proposal, checking, and retention procedure, make one task-skill revision S1. Run exactly two child checks: the milliseconds target and the passing seconds case. Preserve the comparable parent records. A task-skill revision is eligible only when both internal checks pass. Copy the unchanged updater to a new filename and compare bytes; do not pretend the rename changes its mechanism. The two prerequisite captures are outside the two child checks and must stay visible in accounting.

## Prepare the second prior update trace

10.17 starts from two prior update traces. Prepare the missing second one explicitly under the same frozen v0. Capture one parent failure involving minutes, then let the actor propose S2 and run two child checks: minutes and the earlier milliseconds case. This costs three additional prerequisite decisions, zero fits. The two actual update traces, not imagined history, motivate the slower meta-level change.

## 10.17: a later matched task-skill round

After reading both traces, apply v0's diagnosis and proposal process to its own instructions. Save before-action identities, one candidate META-SKILL-v1, the reason, added cost, and a counterexample. The updater edit may change proposal guidance and internal check selection. It must not change the external outcome rule or fixtures.

Both later arms start from the identical S2 bytes. Each active updater is frozen for its arm. Each receives one task-skill proposal for a newly reported microseconds case and two internal checks. v0 uses target plus a known passing regression case. The candidate v1 may use target plus an unknown-unit boundary case and require a matching refusal instruction. The same coding agent authors both proposals; separate folders do not erase context. No independent-agent superiority claim is permitted.

After internal selection, evaluate both proposed task skills on the same three external constructed cases: microseconds, unknown ticks, and known seconds. Thus the later comparison has four internal decisions plus six external decisions. Retain v1 only if its produced task skill passes all three external cases, exceeds v0's external pass count, and both arms used their identical declared allocation. Otherwise retain v0. Preserve internal promotions even when the external check exposes a weakness. These exposed fixtures are a diagnostic comparison, not a protected test or statistical result.

## Counts and limits

Seventeen decision executions total: two initial prerequisite captures, two 10.16 child checks, three second-trace prerequisite checks, and ten later checks. Zero fits. Updater and task hashes identify the instruction read at each check. Hashes alone do not prove every prose instruction was followed; retain actor diagnosis and proposal notes too.

Run a separate labelled schedule simulation over eight task rounds with updater horizons one, two, and four. Use invented accounting units of one per task round and two per meta update. Report only update counts and hypothetical cost. No quality, adaptation speed, or noise outcome is measured by this simulation.

Do not reproduce the source paper's five-agent pipeline, cross-branch retrieval, learned allocation, graph search, or benchmarks with these small fixtures. Keep the original source reading, procedure snapshots, failures, outputs, scripts, and publication checks. Preserve the existing infographics.
