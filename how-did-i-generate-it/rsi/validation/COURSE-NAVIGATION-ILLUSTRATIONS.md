# Course, research-studio, and capstone maps

The user prioritizes a visual preview for early feedback before full execution verification. The actual main-overview-v2 image is the style reference. These maps describe the curriculum, not achieved results.

## Generation discipline

The user reported waste from repeated faulty generations. From this correction forward, check the lesson mechanism, exact labels, relations, version identity, and claims before each first call. Aim for one usable draft. Combine substantive corrections into one edit. Allow at most three outputs per figure; if still wrong, preserve it as a draft, state the unresolved issue, and revise the specification before proposing further generation. Do not spend calls on cosmetic alternatives. This rule does not retroactively erase earlier four-version figures.

## Whole-course mindmap

Two versions. V1 mislabeled the generated harness as RSI before recursion was introduced, used unclear ontology relations, and obscured inherited use. V2 corrects these to a generated harness, two explicit model relations, and a revised improver used in later work. Full-size review accepted v2. The six clusters contain all twelve theme numbers. Research labels are examples, not the whole inventory. Map edges group ideas; they do not prescribe execution dependencies.

## Research studio

Three versions. The source is the thirteen groups in scripts/research-groups.mjs and their thirty-eight labs. V1 invented a modular component list and simplified a procedure into a bypass around a check. V2 corrects the five modules and updater actions but leaves an ambiguous connector. V3 removes that connector and uses a four-step numbered procedure notebook. Full-size review accepted v3. The four areas include group IDs 00 through 12. The studio map is an overview; individual method figures and captions carry precise evidence boundaries.

## Capstones: review before correction

Source checked: all five definitions in scripts/lessons-capstone.mjs. The 11.02 limit is two generations and eight CPU fits, with an additional declared inference limit. Task transfer, agent portability, and compute portability are separate dimensions. An audit can withhold support; a peer session must be actual.

V1 preserves the five labs and portfolio, but its harness points back into input data, and Candidate I1 points straight into a later round without an explicit comparison and retention decision. The audit label also presumes a supported claim. Do not publish this draft as selected.

Before v2: keep the composition; correct the input arrow; replace only the ambiguous recursive panel with four numbered stages: propose candidate, run matched trials, compare outcomes and cost, keep or reject. Changed-rule use belongs inside the provisional I1 trial. Label the audit as a claim to examine. No scores or successful outcome are supplied. This is one combined substantive correction, attempt two of three.

V2 repairs the recursive panel and audit, but turns the data connector into a two-headed elbow. The third and final call removes only that connector. Full-size inspection of v3 confirms the removal, retained four-stage comparison, correct lab IDs and budget, and empty portability checkboxes. V3 is selected. No fourth attempt is planned. The surrounding caption states fixed external comparison, matched resources, provisional use before retention, inference limits, and the illustrative status of records.

## Integrated reading path

The main README now states objectives, prerequisites, and approximate duration near its introduction. The exact lab duration fields total 49.8–82.0 hours of author-estimated reading and discussion; independent work and setup are extra. The guided course map adds all twelve theme bridges and readiness checks, all thirteen research groups, and all 101 lab rows. Every lab links to its theme position and the whole-course mindmap. Every theme includes the mindmap, either directly or in a disclosure; ten also have a relevant focused illustration or destination map. Graph engineering and measurement still need their own focused theme figures.

The source index links all 101 READMEs, intent BRIEFs, and authoring modules, plus seven shared skills. It does not imply a separate native skill package for every lab.

These are authoring and full-size image checks. Published-page rendering, full execution, learner assessment, and remaining focused illustrations stay pending.

## Publication checks

The publishers ran successfully: 101 lessons, twelve theme definitions, 101 source-index entries, twenty visual-guide entries, and forty-five generated PNG records with twenty byte-identical selected copies. The teaching-support inventory reports 101/101 authored support and worked examples. The course checker reports 101 lessons, 3,122 local links, and zero publication problems.

The first skill-validator invocation used Windows' default CP1252 decoding and raised a UnicodeDecodeError while reading existing Unicode prose. Re-running the unchanged validator with Python's `-X utf8` succeeded: “Skill is valid!” All seven installed skill files match the canonical package by SHA-256. No package contents were changed to work around the encoding error.

No ML exercise or full runtime suite was run for this image/navigation checkpoint. These checks do not establish successful student execution or universal agent portability.
