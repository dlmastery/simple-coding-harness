# Generated harness repair protocol

Lab 10.31 begins with the existing generated wine package from the clean journey, its brief, its generation provenance, and a historical result. The brief requires both class recalls. The historical report gives a confusion matrix and balanced accuracy but does not state either recall explicitly. This is a report-completeness gap, not evidence that the model predicts incorrectly; a reader can derive the recalls from the matrix.

Read both papers' methods, experiments, resource settings, and limits before the local comparison. Preserve a two-source map. Recover the exact builder skill bytes named in the package's provenance from Git history. The current skill has a later revision and must not be presented as the original builder. No builder is regenerated, edited, or evaluated in this activity.

Use two matched parent/child executions, one wine linear/all seed-17 fit in each, with the existing pinned data and split. The declared lab allocation is one fit per arm, two total, including failures. The historical generated wrapper has a two-attempt per-workspace ceiling; this driver imposes the stricter single-execution allocation. It must refuse to rerun either arm. Each command has a 60-second timeout. No final evaluation or extra candidate.

First execute the unchanged parent package in a new output folder. Run the required prediction check and inspect its actual report for explicit class-0 and class-1 recalls. Require finite values matching recomputation within 1e-10, with unchanged balanced accuracy. Preserve the expected report-completeness failure. Do not count a missing field as zero recall.

After observing that failure, revise one generated component: its report completion step. The child adds both recalls calculated from the saved prediction rows; it does not change the estimator, preprocessing, metric, data, split, shared runtime, or checker. Execute the child once and run the same prediction and report checks. These checks verify the two allocated fit outputs; they do not fit additional models. Require identical prediction bytes between arms as an additional comparison.

Keep original package bytes and the one-component patch. The builder's identity must remain unchanged across both executions. Its historical identity establishes provenance, not that a generator ran in this study. The current author performs diagnosis and revision in one shared context. No autonomous generator improvement, independent creator/executor comparison, paper reproduction, or fresh-task transfer is established.

Propose, but do not execute, a separate B0/B1 generator comparison on two new briefs. Include failed generations and equal resource limits. Learner prediction, quiz, and teach-back remain unattempted. Retain subprocess costs, failed report checks, and unavailable author/inference costs.
