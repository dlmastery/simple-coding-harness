# Closed-study learner entry check

Allocation: one read-only diagnostic, one existing-output refusal and one
relative-path refusal. Zero model fits, source rewrites or final rescoring jobs.
The diagnostic recomputes metrics from archived predictions and checks their
manifest identities. It does not query a hidden evaluator or change a model.
Use a fresh sibling workspace. Do not modify the completed experiment.

Compare OUTCOMES.csv and INPUTS.csv with the earlier retained diagnosis.
Inspect spam as a gain, DNA as a tie and Miami housing as a regression.
Record skips for learner predictions and questions. This is a maintainer
walkthrough of the student entry path, not a student comprehension assessment.
