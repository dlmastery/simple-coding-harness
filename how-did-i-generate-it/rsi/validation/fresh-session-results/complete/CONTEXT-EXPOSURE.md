# Context supplied and exposure

The host supplied the repository path and commit 0132b2302e3e0212fb233f229322a09e8f2c2a88, the complete packet path, the exact fresh sibling output path, and a bounded maintenance request: exactly one baseline attempt, one-attempt allocation, project-local Python, a real 60-second subprocess deadline, course checker, no retry or final evaluation, preserve failures and records. No expected numeric score was supplied. The host stated sources were frozen before dispatch. Shared filesystem isolation is procedural, not a security sandbox.

The inherited system/developer context includes available tools, skill catalog, operating system, date/timezone, workspace paths and general agent rules. No parent conversation, score target, previous run result or learner response was supplied. This is maintenance mode. Learner prediction, quiz and teach-back responses are skipped; none are invented. The handoff's fixed baseline skill is being applied directly; no learner tutoring session is being conducted.

Before creating the output directory, five read-only command groups ran successfully (all shell exit 0). They listed the repository root and the complete packet's three filenames; read HANDOFF.md, BASELINE-SKILL.md and TASK.md; checked git HEAD; checked for applicable AGENTS.md files; read rsi/tools/README.md, rsi/AGENTS.md, the bike DATA-CARD.md and rsi/tools/requirements.txt; and inspected necessary implementation portions of lab.py and check_result.py. The skill checksum matched the handoff.

A discovery mistake occurred: `rg --files rsi | rg '(data.*card|DATA|data_card|AGENTS\.md)$|bike'` listed historical evidence filenames while locating the bike data card. Its combined tool output was truncated (15,408 original tokens). Historical filenames, candidate labels and model names therefore entered context. No historical evidence content, score, prediction, parent record or other packet was opened. This limits any strict claim that only the packet and necessary paths were exposed. The transcript retains the visible command output; no claim is made that the truncated discovery stdout was archived in full.

The tools README contains links and references to historical evidence and other exercises. Those links were not followed. Necessary lab.py reads expose the public split rules and implementation, including final-evaluation function source, but no final evaluation was invoked. The required data inspection summarizes all public partitions, including final targets; this is disclosed and is not a secret held-out evaluation boundary.

Runtime command stdout/stderr and exits are preserved separately under commands. Source/data/dependency identities are recorded before the baseline. No repository changes, extra fits, publishing, messages to third parties, or subagent delegation are authorized.
