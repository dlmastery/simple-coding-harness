# Read and action ledger

Repository root used for repository commands: `C:\Users\abhir\Documents\Codex\2026-09-19\lo\work\simple-coding-harness`.
Output root: `C:\Users\abhir\Documents\Codex\2026-09-19\lo\work\rsi-fresh-complete-2026-09-23`.

All six initial shell command groups returned exit 0. Read-command stdout was returned in the tool transcript, not separately captured on disk. The third output was truncated; see CONTEXT-EXPOSURE.md. This ledger records the commands and known reads, not a fabricated complete stdout capture.

1. `Get-ChildItem -Force -Name`; `Get-ChildItem -LiteralPath 'how-did-i-generate-it/rsi/validation/fresh-session-plan/complete' -Force | Select-Object Name,Length`.
2. `Get-Content -LiteralPath` on the complete packet's HANDOFF.md, BASELINE-SKILL.md and TASK.md; `git rev-parse HEAD`; conditional `Get-Content` on repository `AGENTS.md` and `..\AGENTS.md` (neither existed).
3. `Get-Content -LiteralPath 'rsi/tools/README.md'`; `rg --files rsi | rg '(data.*card|DATA|data_card|AGENTS\.md)$|bike'`; `Get-FileHash -LiteralPath 'how-did-i-generate-it/rsi/validation/fresh-session-plan/complete/BASELINE-SKILL.md' -Algorithm SHA256`. Conditional existence/read checks were made for `C:\Users\abhir\AGENTS.md`, `C:\Users\abhir\Documents\AGENTS.md`, `C:\Users\abhir\Documents\Codex\AGENTS.md`, `C:\Users\abhir\Documents\Codex\2026-09-19\AGENTS.md`, `C:\Users\abhir\Documents\Codex\2026-09-19\lo\AGENTS.md`, repository `how-did-i-generate-it\AGENTS.md`, `how-did-i-generate-it\rsi\AGENTS.md`, `how-did-i-generate-it\rsi\validation\AGENTS.md`, `how-did-i-generate-it\rsi\validation\fresh-session-plan\AGENTS.md`, `how-did-i-generate-it\rsi\validation\fresh-session-plan\complete\AGENTS.md`, `rsi\AGENTS.md`, and `rsi\tools\AGENTS.md`. Only `rsi\AGENTS.md` existed and was read.
4. `Get-Content -LiteralPath 'rsi/examples/bike-demand/DATA-CARD.md'`; `Get-Content -LiteralPath 'rsi/tools/requirements.txt'`; `Get-Content -LiteralPath 'rsi/tools/lab.py' -TotalCount 110`; `Get-Content -LiteralPath 'rsi/tools/check_result.py' -TotalCount 90`; `Get-ChildItem -LiteralPath 'rsi/examples/bike-demand','rsi/examples','rsi/tools' -Filter AGENTS.md -Force` (no further instructions found).
5. `rg -n 'def inspect|def run|def contract|add_argument|__main__|def main|fit\(' rsi/tools/lab.py`; `Get-Content -LiteralPath 'rsi/tools/lab.py' | Select-Object -Skip 340 -First 190`; `Get-Content -LiteralPath 'rsi/tools/lab.py' | Select-Object -Skip 645 -First 85` (last selection empty).
6. Set taskOutput to the exact output root; `if (Test-Path -LiteralPath $taskOutput) { throw 'Refusing existing output directory' }`; `New-Item -ItemType Directory -Path $taskOutput -ErrorAction Stop | Select-Object FullName`; `Get-Content -LiteralPath 'rsi/tools/lab.py' | Select-Object -Skip 217 -First 110`.
7. Applied a patch creating only output-root driver.py, CONTEXT-EXPOSURE.md and PROGRESS.md. The full driver source is retained. Invoked repository `.venv\Scripts\python.exe` with absolute output `driver.py preflight`. The shell redirected stdout and stderr to preflight.stdout.txt and preflight.stderr.txt, saved LASTEXITCODE in preflight.exit.txt, and returned that exit. It was 0.
8. The preflight driver hashed the packet, rsi/AGENTS.md, tools README/requirements/lab/checker, bike data card/hour.csv/archive, Python executable and driver. It obtained Python/platform/installed-package metadata, ran `git rev-parse HEAD`, and invoked the prescribed data inspection. Exact argv, cwd, deadline, timestamps, PID, stdout, stderr and exit are in commands/01-commit and commands/02-inspect. No fit occurred.
9. Read only newly generated DATA-REPORT.md, dependencies.json, preflight.stdout.txt, preflight.stderr.txt and sample.csv using `Get-Content -LiteralPath` from the output root. Inspection was complete before invoking the fit.
10. Invoked repository `.venv\Scripts\python.exe` with absolute output `driver.py attempt`. The shell redirected stdout/stderr to attempt.stdout.txt and attempt.stderr.txt, saved LASTEXITCODE in attempt.exit.txt, and returned it (0). This ran exactly one `lab.py run`, with constant/calendar/seed 17/attempt-limit 1 and the packet's policy file, then one course `check_result.py`. Commands/03-baseline and commands/04-check preserve complete argv and process evidence. Driver reads of source/data files for hashing and the course tool's own reads are limited to the named pins. The tool loads the public hourly data table, fits train rows only, and saves selection predictions. The checker reads this workspace's ledger/result/predictions and pinned source targets. The post-attempt identities were then written and progress updated.
11. Read only newly generated trials.csv, trial-001/CHECK.md, commands/03-baseline/exit.json, commands/04-check/exit.json, CONTRACT.md, attempt.stderr.txt and both model/checker stderr.txt files. All three stderr files were empty.
12. Created this ledger and finalize.py in the output root. finalize.py reads this run's dependencies, before/after identities, trial ledger, process records, generated output files and invokes a path-scoped `git diff --exit-code HEAD` on the allowed source pins. It writes a Markdown identity report, final report, progress, and output hash inventory. No model fit or final evaluation occurs. Finalization stdout/stderr and process exit are captured as finalize.stdout.txt, finalize.stderr.txt and finalize.exit.txt. The final verification reads only FINAL-REPORT.md, PROGRESS.md and finalize.stderr.txt.

The Markdown records deliberately distinguish actual process evidence from unarchived initial read outputs. They do not claim a sandbox, independent evaluator or proof against a malicious agent.
