# Composition and checked references

Preflight: 20 September 2026. Individual figures for labs 10.35 and 10.36 use original classroom examples from [the frontier lesson source](../scripts/lessons-frontier.mjs). Save exact prompts first; use the approved generator with the main overview as the visual reference.

## Method checks

[MetaRSI v2, sections 4.1–4.5](https://arxiv.org/html/2609.06396v2) separates data, harness, and model write surfaces. Its scheduling layer changes operator order and policies; the meta layer revises the scheduling policy under protected boundaries. The outermost meta agent is fixed. Our much smaller simulation illustrates versioned evidence and later use of a changed scheduler; it does not reproduce every nested role or transition adapter. The pictured Q1 interface-check rule is a classroom invention, not a quotation or paper result.

[HarnessEvolve, sections 3.2–3.5](https://arxiv.org/html/2609.00829v1) verifies answer-conditioned references before using them for diagnosis. Candidate changes face quality and performance checks, including prior cases. The local exercise adds a legitimate alternative-path counterexample to make clear that divergence alone is not proof of error. It does not reproduce the source’s thresholds, training sets, or benchmark gains.

## Locked mechanisms

10.35: five small schedule checks; three stub operators with disjoint write surfaces; stale evidence and undeclared writes rejected; external evaluator unchanged. The conditional accepted-Q1 path must precede later reading and execution of its new rule. Keep rejection separate and do not assert measured benefit or actual LLM training.

10.36: four trace checks and one candidate skill evaluated on two fixtures, no fits. The known-answer shortcut cannot become the usable reference. A general skill edit must pass a quality check and current/prior fixture evaluation before promotion. Alternative successful orders are valid only in the constructed audit fixture, not all workflows.

Maximum three outputs per figure. Preserve every original, including rejected drafts. Full-size reviews and publication results follow below.

## Full-size review

Selected operator-composition-v1 on its first attempt. Disjoint write surfaces, exact stale-version mismatch, a fixed external evaluator, conditional activation, and later execution of the same Q1 rule are visible. The three upper rows are independent demonstrations, not a simultaneous trajectory. Marks beside checks describe required operations, not measured passes. The full caption states the simplified architecture and unexecuted simulation scope.

Selected checked-reference-v1 on its first attempt. The known-answer shortcut has no path into usable references. General instruction quality precedes current/prior fixture checks and the unresolved retention decision. The caption states that a failed quality check stops immediately and that distinct fixtures do not establish isolated agent contexts. The two alternative audit orders show why a first divergence is only a lead. Trace verdicts are constructed examples, not a new recorded run.

Both original outputs and selected copies are retained unchanged, with exact prompts. No corrective generation was required. Publication checks are shared with the evidence/economics batch.
## Publication results

Shared four-figure checkpoint: 101 lessons published, 42 selected gallery figures, 71 archived outputs with every selected copy hash verified, and 3,577 local links checked with zero publication problems. Generated figures map to 38 of 101 labs; 63 gaps remain. Git whitespace validation passed. No runtime, browser, or learner acceptance is inferred from these checks.
