# Controlled walkthrough checks

Executed on 20 September 2026, Windows, repository-local Python 3.12.12.

| Command through the local Python environment | Observed outcome |
|---|---|
| `run_tests.py rsi` | 15 tests passed in 3.20 seconds; 101 lessons; 1,249 local targets; zero publication problems before these index additions |
| `how-did-i-generate-it/rsi/scripts/run-inheritance-walkthrough.py` | Completed two checks: false summary rejected, valid improvement retained |

The foundation walkthrough previously completed nine fits plus one final refit; its outputs are preserved. The inheritance driver makes no new fits. It copies predictions from those actual runs and labels the altered report explicitly. Its output folders refuse overwrite.

These are author executions and component checks. Student learning, other native agent sessions, independent improver contexts, GPU training, and cluster submission remain untested. Model-fit timing does not measure total agent cost.
