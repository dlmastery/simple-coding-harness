# Keep knowledge separate from the instructions being tested

Declared 21 September 2026 before execution. New sibling workspace: rsi-work-2026-09-21-memory-interfaces. This walkthrough covers 10.27 and 10.32. Zero model fits or parameter updates. Preserve the existing conceptual illustrations.

## 10.27: two checks and a separate exposure condition

Use the actual failed unknown-unit task proposal from the meta-skill archive as prior evidence. Copy its proposal, input, result, and conversions. Keep a TRACE.md wrapper and source hashes fixed. The accepted active skill is the later refusal-capable task skill. A source-linked notebook records the failure without becoming the task actor's instruction.

Propose one atomic fallback change: treat an unknown duration unit as seconds rather than refusing. This deliberately revisits a known failure, not a newly discovered idea. Test the proposed skill on a valid seconds case and an unknown-ticks case, exactly two checks. The evaluator's fixed rule requires a correct candidate for valid data and refusal for an uninterpretable unit. Reject if either check fails. Keep the accepted active skill byte-identical, retain the failed child, and append a scoped result to the notebook.

Instrument the scripted evaluator's reads. It receives the proposed skill snapshot and case input, not the notebook. The author can read every file and already knows the failure, so this is not technical isolation of the coding agent. In a separate condition, let a scripted actor read the active skill and notebook and record both reads. This is one exposure-only action, no third task answer or fixture check. Explain that the inference interface has changed without asserting a measured performance effect.

## 10.32: five actual author answers and exact checks

Create eight inventory events: add 7, remove 2, add 4, remove 3, add 5, remove 1, add 2, remove 4, starting from zero. The checkpoint follows event four. Save a correct checkpoint summary and an explicitly faulty summary that omits the fourth event. The exact checker derives the final count from the original event CSV; it does not trust an answer in a packet.

Prepare five packets: raw history; correct checkpoint summary plus tail; faulty summary plus tail; raw history with longer irrelevant descriptions; correct summary plus an equally expanded tail. Expanded descriptions must preserve every state-changing operation. Each condition receives one packet and one answer of at most 60 words. The author answers from the supplied representation, then a separate check scores the answer. The driver does not generate answers.

This same author designed the sequence and sees all conditions. The incorrect-summary condition demonstrates what its supplied representation implies; it is not an independently sampled memory failure. No private answer key is included in a packet, but the author and filesystem are not technically isolated. Do not call the conditions a clean memory ablation or claim a general language-model performance effect.

Record packet character and whitespace-word counts, answers, source hashes, and exact outcomes. These lengths are not provider tokens. The expansion comparison tests this one supplied sequence; it cannot establish long-context scaling or training effects. Seven total task checks across the two labs, plus one separate exposure read. Learner predictions, teach-back, and quizzes remain unattempted in this author walkthrough.
