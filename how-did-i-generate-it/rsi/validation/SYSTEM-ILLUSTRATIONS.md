# System-intelligence illustration review

21 September 2026. Read the five system lessons, individual teaching guidance, fixed-route table, actual context packet, ablation protocol and six outcomes, and the fixed-component execution description. Preserve 05.04's existing handoff illustration. Use the user-approved built-in generator with the main overview as the craft reference; its model identity is not exposed.

## Preflight

- 05.01: fixed agent procedures coordinate components; the ordinary task model still fits a training median. Input and domain checks precede fitting. Output checking needs reference targets and row IDs as well as predictions. One valid fit; the invalid fixture stops. The optional removed-guard exercise must use a fit stub and report overlapping protections.
- 05.02: two fixed routes and separate contracts/workspaces. Bike regression uses MAE; the binary wine target is quality at least 7 and uses balanced accuracy plus both class recalls. Training majority gives balanced accuracy 0.5 only when both classes are represented in evaluation. Unknown or missing-type requests stop without fitting.
- 05.03: distinguish rules, references, and run state. The example contract allows two attempts and its ledger charges one; one remains. A stale three-remaining note cannot refill the budget. Two context checks, no fits; packet links to authoritative current sources.
- 05.05: the measured fixture design has two overlapping guards. Four main executions compare full versus domain-removed on the same valid/leaked inputs. A separately declared two-case follow-up removes both. No actual model is fitted. Illustrate the expected mechanism with explicit labels, not invented result plots. Clarify the lesson budget to include the separate follow-up.

Save exact prompts before generation, review all arrows, labels, arithmetic, guards, and fixture counts, and preserve every output. Aim for one output per figure; combine material corrections within the three-output maximum. No full execution-verification phase has resumed.


## Review and integration

- 05.01 selected fixed-components-system-v2. The first draft invented numerical tables, varying predictions despite the constant median, and daily task wording. One combined revision replaced those cells with blank rows, used source_row and the task’s actual field names, and restored hourly wording. The training, selection-input, and checker-target roles now agree with the lab. Four other component details remain illustrative responsibilities rather than proof that a general validator enforces every conceivable rule.
- 05.02 selected fixed-task-routing-v1 on the first attempt. Correct targets, baselines, metrics, rejection route, separate workspaces, and conditional recall arithmetic. Caption identifies the bottles as a laboratory motif; the task still uses pinned red-wine data. No plot or score is presented as a new measurement.
- 05.03 selected context-rules-knowledge-state-v1 on the first attempt. All three context roles, the 2−1=1 budget, exclusion of wine instructions, stale-claim rejection, and blank actual-results cells are correct. Caption requires identity and active-work reconciliation before accepting a ledger as current.
- 05.05 selected ablation-overlapping-checks-v1 on the first attempt. The three system configurations, four main versus two follow-up executions, two identical input fixtures, and redundant-check truth table agree. The table is explicitly illustrative expected behavior; no real fit runs. The red final Yes means lost protection.

Copied each selected image into the course and preserved all five originals and prompts. Embedded full-size links, alt text, and captions while preserving 05.04. Rebuilt the gallery, manifest, coverage, lessons, briefs, and activity inventory. The original 05.05 evidence already contains six outcomes under its separately declared follow-up; no new runtime coverage is inferred from editing the budget text. Full activity, learner, other-agent, backend, and published-page review remain deferred.

The main README still said seventeen images. Replaced that stale snapshot with links to the visual guide and generated per-lab inventory, keeping status authoritative without another manual counter. The prior README remains in the verified parent commit and original course backups remain preserved.

Publication checks passed: 101 lessons, 4,025 local links, zero problems; 132 generated PNGs registered and all 87 selected course copies hash-verified. Infographic coverage is 83/101, with 18 remaining. The activity inventory remains 63 related mappings and 38 unmapped labs, not completed-lab counts. No model fits or new learner executions were performed.
