# Reconcile the four starting labs

Declared 22 September 2026 before new checks. Use the original clean-journey 00.01–00.04 artifacts; do not fit again. Preserve the historical reports and their limits.

The original artifacts cover source-row inspection, the retrospective task, data inspection and one fixed baseline. Missing from that folder are the separate tomorrow-at-noon brief, the disabled-command explanation, quiet/busy error comparison, and the deliberately false summary check. Complete these author activities in a new sibling workspace.

Budget: zero fits. Read pinned source and saved predictions, inspect three errors plus quiet/busy examples, and check the same MAE against two reports. Generate a separate checker; run it twice under 60-second limits. Original report must pass. A labelled copy whose sole content edit changes the six-decimal score to 10 must fail with nonzero exit. Verify source hash, candidate recipe, complete selection identities and targets as part of the check. Save commands and failures. This checks one metric against two reports; it does not authorize another model or candidate.

The future task requires a genuine forecast source before weather can be used. A text-only agent can explain a process but cannot claim command execution. Record both distinctions without pretending to alter the host's permissions or produce a forecast.

Reconcile every named starting-lab step and additional change to a concrete file. Inspect the existing data plot and conceptual figures; preserve good assets. Learner predictions, quiz and teach-back remain unattempted. Record original identities, code, observations, costs and archive manifest before publication.
