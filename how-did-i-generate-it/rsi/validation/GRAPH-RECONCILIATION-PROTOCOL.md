# Check graph decisions without new model training

Declared 22 September 2026 before execution. Supplement labs 03.01, 03.02, 03.04 and 03.05 in a new sibling workspace. Preserve their earlier records. Allocate zero fits and no final evaluation.

- 03.01: preserve the existing editable, artifact-labelled workflow and rendered image. Execute a generated ordering checker on the normal order, check-before-fit order, split-before-inspection under the original edges, and that same order under a copy missing inspect → split. The last case should pass the weakened graph but remain scientifically under-specified.
- 03.02: preserve valid and missing-target data fixtures, and declare an absent-evidence path. Execute three safe routes plus an explicitly unsafe copy where unknown means pass. Routing readiness is a decision only; never call a fitting tool. A target-presence test is not comprehensive data validation.
- 03.04: use two report fixtures missing a candidate ID to match the current lesson and illustration. Keep the validator unchanged. Allow at most two repair slots per fixture, four total; expect the effective repair to use one and the ineffective repairs to use two. Save every report, verdict and counter state. A no-feedback extension is an unexecuted prediction, with no third fixture or extra repair allocation.
- 03.05: copy a known checked baseline. Execute a deliberately failing report writer, then repair only that operation. Run one separate prediction check. Preserve prediction and ledger hashes. Compute the changed-split descendants without executing them. Test the report dependency guard with original predictions and with a copy changing exactly one prediction value; both records stay available. No refit is permitted.

Record source identities, concrete inputs, outputs, verdicts, expected exits, command timings and an ordered event journal. Label fixed rules and constructed fixtures. Use 60-second command timeouts and keep any failed unexpected attempt. Review the existing conceptual figures and diagram, preserve them, archive the original artifacts, and update the four lesson examples and their activity coverage. No learner answers, independent agents, real scheduler execution or adversarial isolation are implied.
