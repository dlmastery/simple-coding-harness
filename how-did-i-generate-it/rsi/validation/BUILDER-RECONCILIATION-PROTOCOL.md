# Inspect the brief, generated package and its runtime boundary

Declared 22 September 2026 before new execution. Supplement 06.01–06.05; preserve their historical packages and records. The earlier generation was author-guided template specialization, not autonomous builder improvement. Independent regeneration remains outside this pass.

Allocation: one new actual bike baseline fit in a fresh 06.04 workspace with two admitted slots. The second slot is consumed by the requested leaked-feature failure before training. A third distinct recipe is refused on exhausted budget. No other fit or final evaluation. Every child command has a 60-second timeout. The existing two wine candidates are checked from saved predictions only.

Copy the historical bike package unchanged, retaining its dependency on the current course tools, pinned data and local environment. Add a separately identified evidence-binding checker before execution. This checker compares an explicit expected workspace/candidate/contract/prediction identity with a report claim. Test a matching claim, an actual successful report from another workspace and a claim missing candidate identity. No report claim can authorize a fit; admission remains the controller's separate task.

Audit the historical brief/entry hashes and builder identity records. Keep a metric-omitted brief with an explanation of MAE versus RMSE and the invalidity of post-result metric choice. Make a diagnostic README copy claiming ten attempts while its unchanged code fixes two; execute a source/document consistency check. Do not alter the accepted package to fit that claim.

Map scientific and operating requirements to stated, implemented and executed evidence. Inspect preprocessing and selection source; distinguish source inspection from a fresh dynamic leakage test. Check both saved wine predictions, recompute both recalls and inspect duplicate-group assignment with no new training. Preserve an explicit ordinal-task brief and evaluator impact as an unexecuted extension.

Retain inputs, commands, outputs, refusals, costs, package identities and failures. Inspect the five existing conceptual illustrations and keep them. Archive original files, update the lesson examples and coverage, and checkpoint to GitHub. Student answers, fresh-agent generation, total inference cost and optional cluster execution remain separate evidence requirements.
