# ScienceBuddy classroom execution

The protocol and driver were pushed before execution at `367d333d750b791aedd8470683fdc6c3d1d0854f`. Its dedicated three-platform workflow passed (run `35516270523`). The local command completed with exit 0 in 3.133 seconds; the driver reports 1.929 seconds inside that interval. No new model fit or LLM training occurred.

The [published evidence](../../../rsi/evidence/2026-09-20/sciencebuddy-laptop/README.md) retains all 63 copied workspace files and a verified manifest. The six report verdicts match the declared cases. Complete evidence passes under the child reporting procedure; an omitted minority value fails; missing minority rows remain unresolved. The honest refusal has a separate status from completing the requested task.

All three numerical cases completed with valid probabilities and analytic/finite-difference agreement within 8e-11. Equal rewards produce no update. The single wrong reward raises a wrong action's probability, while aggregate true expected reward still rises less than in the correct-reward case. The recorded interpretation avoids claiming an unobserved aggregate regression. The measured numerical chart was visually inspected.

The paired simulation evaluates four invented entries once, caches them, and records a harness preference reversal across model labels. No placeholder is described as training. The result arithmetic uses separately labelled paper inputs. The primary-source audit identifies the inspected sections and source inconsistency; it is separate from measured classroom outcomes. Additional GWAS reasoning, a longer instruction variant, and the larger-training plan are retained as reading/planning outputs, with no extra hidden report or training runs.

No actual student, domain expert, independent agent context, GPU backend, or paper reproduction was tested. The reporter is deterministic under an author-written interpreter. Source inputs remain publicly accessible to the host. The full course goal therefore remains open even though these local activities ran.
