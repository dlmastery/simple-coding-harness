# README snapshots before restoring visual navigation

The two `.md.snapshot` files preserve the previous root and RSI README bytes. Their original relative links belong to their original directories. Git history also retains both versions. Existing infographic bytes were not changed.
