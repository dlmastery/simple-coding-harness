# Exploration baseline correction

21 September 2026. Inspecting lab 10.03 before execution exposed a factual error in selected figure `broad-probes-focused-test-v2.png` and its caption: both say training mean. The actual bike constant estimator in `rsi/tools/lab.py`, function `pipeline`, uses `DummyRegressor(strategy="median")`. The course's MAE baseline explanation also uses the median. Do not change the implementation to match the incorrect picture.

The original image was visually inspected. Preserve v1 and v2. Request one targeted edit for v3; no redesign or additional cosmetic variation. The existing generated illustration style remains unchanged. The user already authorized the available generator instead of an unavailable Imagen selector.

## Exact edit prompt

Edit the supplied professional white-background scientific watercolor infographic with two local text corrections only. In the upper middle-left panel headed “Fit 1 · Constant baseline”, replace “Training-set mean” with “Training-set median”. In the small blue outlined box directly below it, replace the Greek mu symbol with a lowercase italic “m”, representing the median. Preserve every other word, arrow, object, color, title, panel, dimension, texture, and composition. Keep the full original image uncropped and preserve its high-quality drawing and legibility. Do not add measured results or redesign anything. This corrects the baseline statistic to match the actual MAE exercise.

## Review criteria

The two requested labels must read median and m. Check the three-fit limit, two probe arrows, decision-before-fit order, weather-category wording, final-evaluation boundary, and blank result table against v2. Preserve the raw tool output and selected copy with matching hashes. No claim about the generator's model identity is permitted without returned metadata.

## Observed result

Accepted the first targeted edit as v3. Full-size review confirms “Training-set median” and italic m. The three-fit allowance, probe arrows, decision-before-fit sequence, weather-category label, blank results, and final-evaluation boundary remain intact. Raw output: `exec-787a0534-ae46-4da5-9d03-248eb6646e00.png`. The built-in tool did not expose its model identity. Selected and archived copies are checked by the illustration manifest. V1 and v2 remain preserved; no further variation was requested. Inventory: 155 archived versions, 105 selected illustrations, 101 individual lab figures.
