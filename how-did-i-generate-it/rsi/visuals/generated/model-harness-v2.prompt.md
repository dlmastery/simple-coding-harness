# Model–harness co-evolution · revision v2

Input: `model-harness-v1.png`, inspected before editing. Reject the added paragraph that conflates training evidence with held-out evaluation; explicitly separate those routes. Remove other unrequested dense prose.

```text
Correct the scientific boundaries in this infographic. Preserve the three versioned pairs exactly: (H0,M0), (H1,M0), (H1,M1). Preserve their update arrows, the human-input source cards, overall white-background illustrated style, title, and local-lab notice.
Make these precise changes:
1. DELETE the entire bottom-left paragraph box that starts "The same tasks and rubrics provide training evidence". It wrongly conflates training and held-out evaluation.
2. In that cleared area, place a simple separate blue document labelled exactly "Held-out evaluation cases". Connect it ONLY to the bottom "Fixed external evaluation for each comparison" strip. No connector from this document to the optimizer or training evidence.
3. DELETE the entire bottom-right paragraph box that begins "Each evaluation record includes both harness". Leave white space there.
4. Under "Declare training evidence", DELETE the explanatory sentence "Human-grounded input, not a model pretending to be a human." Do not replace it with any text.
5. In all THREE teal evaluation cards, keep the correct pair heading and Evaluation report label. Replace all bullet lines with exactly "Pair identity", "Held-out results", "Total cost". No extra bullets or paragraphs.
6. On all model chips, replace "Training weights" with "Model weights".
7. The dashed Training evidence connector must end with an arrowhead AT the Parameter update / optimizer, not at an evaluation card. Keep it visually separate from the held-out route.
Do not introduce any other explanatory text, footer, scores or charts. The essential distinction is training evidence enters a parameter update; held-out cases are used only in fixed external evaluation.
```
