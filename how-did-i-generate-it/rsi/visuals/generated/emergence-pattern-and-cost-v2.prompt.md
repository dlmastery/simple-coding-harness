Edit this emergence infographic to remove a semantic collision, preserving all its layout, style, connectors, shared inputs, equations, empty result fields, and lower counterexample.
A and B are JOB TYPES ONLY. Keep the top shared-input A/B cards and "A,A,B,B repeated three times" unchanged.
In EACH of the THREE simulation panels, rename "Worker A" to "Worker 1" and "Worker B" to "Worker 2". Rename all corresponding trace-sheet headers to "Worker 1 trace" and "Worker 2 trace".
The machine front plates currently show A and B: replace those labels with 1 and 2 consistently.
The small input trays at the workers currently show A and B, which falsely suggests a preassigned job type. Replace ALL SIX tray labels with "Jobs". Use neutral gray tray faces. These trays must not imply type specialization or predetermined grouping.
Add one concise note beside the shared inputs: "Either worker can process either type."
No other changes. No new values or observed grouping. Preserve "Local preference", "No preference", "Randomized history" and the distinction between job type and worker identity.
