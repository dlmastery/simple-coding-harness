Edit only two Input cells in the middle Data flow table. In the Fit row replace "Training rows + recipe" with "Training rows + recipe + selection inputs". In the Check row replace "A predictions + reference targets" with "A predictions + reference targets + expected row IDs". Wrap the text neatly and keep it legible inside the cells; adjust those row heights slightly only if necessary. Preserve all other labels, panels, candidate A identities, arrows, the stopped trace, and the white-background illustration. No other changes. Here the Fit action denotes the tool operation that fits on training rows and predicts for selection inputs.
