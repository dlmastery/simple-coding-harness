Edit the attached infographic in place, preserving its title, white-background ink/watercolor style, readable text, panels, colors, and all unaffected content. This is a combined correction of three substantive connector errors. Do not add decorative elements, new numerical results, or new claims.

1. In upper-left HarnessDev panel, REMOVE the direct long bent arrow from the amber "Harness changes" notebook to "Held-out evaluation". Keep the short arrow from the amber notebook to "Frozen harness". Add exactly one short downward arrow FROM the Frozen harness TO Held-out evaluation. Reposition the held-out sheet slightly lower if needed for a visible gap and unambiguous arrowhead. The sole evaluation route must be Harness changes → Frozen harness → Held-out evaluation, with no return route. Preserve "Freeze before measuring generalization".

2. In the bottom proposed-follow-up strip, reverse all four arrows connecting a builder and a fresh-brief envelope. Each arrow must point FROM its Fresh brief envelope TO the Builder toolbox. Both Builder B0 and Builder B1 each RECEIVE Fresh brief 1 and Fresh brief 2. The briefs are inputs, not artifacts the builders generate. Keep exactly the two identical brief labels for each builder. Keep "Proposed follow-up: not run" and the final text "Evaluate the systems they generate". Do not add arrows from builders to briefs or between B0 and B1.

3. In the middle local-experiment panel, keep H0 → one component edit → H1. Both parent H0 and candidate H1 must feed "Matched checks". Keep the existing H1 → Matched checks arrow, and add a separate thin blue route from the bottom of H0, below the edit arrow but inside this panel, across to Matched checks, labelled "Parent H0". Route it clearly without touching the lower follow-up strip. The matched check sheet already says Run H0 and Run H1; preserve these labels. The decision remains unresolved, Keep H1 or Reject H1. No path may bypass Matched checks into a keep decision.

Inspect the whole figure after corrections. Preserve all other text and layout as closely as possible. This remains conceptual, not evidence of an actual run.
