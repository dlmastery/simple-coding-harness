Edit the supplied infographic, preserving its beautiful composition, five stations, watercolor craft, white background, typography, arrows, notebook and all main headings. Correct only its technical table content and task wording, with the following coordinated replacements.
1. On the left task clipboard, change "Predict bike rentals for selected days" to "Estimate hourly bike rentals".
2. On the Valid fixture card replace "Historical data and features (no targets)" with "Permitted input features".
3. On the Target-derived input card replace the small description with "Contains target-derived information".
4. Training rows table: use only headers "hr", "weekday", "cnt"; remove ALL dates and numerical values; show blank ruled rows with faint gray nonnumeric placeholder strokes.
5. Selection inputs table: use only "hr", "weekday"; remove ALL dates and numerical values, showing blank ruled rows.
6. Predictions table inside the lens: use exactly "source_row", "predicted"; remove ALL numbers. Blank ruled rows only. In particular NEVER show different numeric predictions from a constant median predictor.
7. Selection targets + row IDs table: use exactly "source_row", "actual"; remove ALL numbers and show blank ruled rows.
Do not add scores, dates, timestamps, invented outputs or new labels. Preserve the explanatory distinction "No LLM weight training. The task model still fits data." Preserve Expected paths, fitting on training only, selection inputs for prediction, refusal before fitting, and the existing connections. This is a conceptual blank-artifact illustration for students to fill with actual evidence.
