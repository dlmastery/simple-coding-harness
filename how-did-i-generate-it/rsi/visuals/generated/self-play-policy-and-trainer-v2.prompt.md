Edit this self-play infographic. Preserve the title, two-phase composition, art style, all correct settings, exact update equation, terminal-return meanings, blank evaluation counts, both 500-game tests, freeze divider, and bottom distinctions. Correct these technical details together:
1. The main training-table interior currently has headers X, O, Value. Replace with FOUR exact columns "Board", "Player", "Move", "Value". Empty ruled rows only. Its title remains "Board • Player • Move → Value". Player X/O are field values, NOT separate columns. Do not invent data.
2. Establish exactly one clear clockwise learning cycle among the four existing objects: main table → Play a game → Terminal return → Update stored values → main table. The table-to-game connector must have ONE arrowhead, at the game end only. The update-to-table connector must have ONE arrowhead, at the table end only. Keep the game-to-return and return-to-update arrows. No second head at the table-to-game source.
3. Remove the entire bottom U-shaped arrow joining Play a game and Update stored values. Keep the text "Repeat for 3,000 games" as a simple caption without a connector. This removes a confusing shortcut.
4. Add a short label beside the return-to-update connection "After the game".
All other existing content stays unchanged. No new numbers, trained scores, or extra learning arrows. The X/O lookup arrows can remain as lookup indications, distinct from the single learning cycle.
