# Check admission before accepting an old success

Declared after the original nine commands completed and before this extension runs. Allocate three additional no-fit checks, zero new model fits and no admission changes.

The original binding checker compares a claim with a supplied expected identity record. Its cases show a matching result, an unrelated workspace and a missing candidate. For the two actual refused requests, the stronger question is whether that request has a successful admitted candidate at all.

Add a separate request-state guard to the copied package. It reads the actual current ledger, rejects a failed candidate and rejects an unadmitted candidate before accepting any claimed success. When the candidate is successful, derive contract/prediction identities from the current files and then compare the claim. This is a cooperative local gate, not authenticated isolation or another MAE calculation.

Execute: trial-001 with its matching claim must pass; failed trial-002 with the retained unrelated successful report claim must fail; unadmitted trial-003 with that same unrelated claim must fail. Preserve all three commands and verdicts. The ledger must remain at one successful fit and one charged failure. The first nine commands and their original checker are preserved unchanged.
