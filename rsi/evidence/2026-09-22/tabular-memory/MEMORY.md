# Experience retained after independent outcome checks

96 attempted development fits are represented, including every source generation. The root coding agent interprets these checked outcomes; the verifier certifies numbers and identities, not the truth of a generalized lesson.

Keep a median reference for regression. Numeric scaling is a model choice, not a universal improvement. Native categorical masks differ from treating category codes as numeric distances. Aligning loss can help but did not repair every task. Target transformations and extra capacity also produced regressions.

The executable retrieval interface uses task kind, categorical fraction, feature count, training rows and the training zero-target fraction. No current run budget, pending action or final labels belong in this store. Freeze this memory when comparing its later effect. Keep later task working state separately.
