# Generate a separate prediction checker

Declared 22 September 2026, before execution. This author walkthrough closes the specific checker-generation gap in lab 01.04. It does not repeat its model fit or replace the historical run.

Use the archived 01.03 constant/calendar bike baseline, seed 17. Copy its contract, registry, proposal, policy and predictions into a new sibling workspace. Record their original hashes. The pinned public bike source remains read-only. Generate a separate standard-library checker from the lesson contract without importing the course solver or checker.

Budget: zero fits and exactly two checker invocations. First check the original prediction copy. Then change only the first selection ID to source row 0, a training row, in a labelled copy. Retain every other prediction cell. The first invocation must pass. The second must return a nonzero status for wrong partition membership. Preserve both outputs and commands. Do not repair the deliberately invalid file.

The checker must validate the source hash, frozen task/split/metric, one successful expected candidate and recipe, policy identity, unique complete selection IDs, source targets, finite numbers and recomputed MAE against the registry. It may inspect the proposal for consistency, but cannot prove which fitting process created predictions. Hashes identify artifacts; they do not authenticate an untrusted writer. Selection is the first half of 2012; row IDs are zero-based CSV data-row positions.

The additional change is an explanation, not a third invocation: a metric number alone cannot establish row identities, target correctness, completeness, model provenance or arithmetic over the missing predictions. Record this limit and leave learner prediction, quiz and teach-back unattempted.

Archive generated code, copied inputs, both outputs, one-cell difference, source identities, environment, costs and progress. Source data already has a pinned repository copy; link it rather than duplicate it. Preserve any failed preparation or execution. Review the existing illustration and align the lesson example with actual outcomes. Run publication and Git-byte checks before the authorized checkpoint.
