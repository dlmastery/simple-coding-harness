# No-fit preflight

186 checks pass. Model objects were constructed and cloned, never fitted. Policy checks use explicitly invented loss values. One worker refusal occurred before reading inputs or fitting. A copied invalid role fixture was refused. Frozen study source and data remain unchanged.
