# A report checked against its rows

The copied 00.03 registry names trial-001, bike, constant/calendar, seed 17, status ok, with score 159.94791188618632. The copied contract names cnt, MAE and the 2011 / first-half-2012 / second-half-2012 partition design. Five original source identities are recorded in SOURCE-IDENTITIES.csv. Their copies are byte-identical.

The generated checker hashes the pinned hourly source, derives the complete 4,358 selection IDs from dates, checks uniqueness and source targets, then recomputes MAE. It compares the registry and report with the same result. Original RESULT.md reports six decimal places: 159.947912. The check accepts the rounding difference with absolute tolerance 0.000000500001. An altered copy changes only that score to 10.000000. Both calls read unchanged predictions and the same checker code.

Actual results: valid exit 0; altered exit 2, with “Report disagrees at declared six-decimal precision.” The two process intervals were 0.1596302 and 0.1408044 seconds. This excludes preparation, author reasoning and provider inference; no model fits or new model predictions. Python 3.12.12 on Windows; standard-library checker. Commands and raw outputs are retained.

The original budget's “one metric” means this same MAE, checked against two reports. Each separate checker process recomputes that metric from the same predictions. The conceptual figure shows the equal mathematical value reused across branches; it is not a literal process trace. The maintained lesson now states the two-report allowance explicitly.

## Correct arithmetic can answer the wrong question

If predictions instead came from training rows, an arithmetic-only checker could correctly average their errors. That number would measure training performance. Labelling it selection performance would still be wrong. Complete expected row membership and source targets are therefore separate checks. This exercise tests a changed summary; lab 01.04 separately tests a substituted row.

The candidate record and proposal are evidence supplied by the same author, not authenticated proof of the fitting process. The historical command/fit record supports the run claim; the new checker only checks the saved artifacts. It establishes no isolated evaluator, unseen-data benefit, independent student performance or RSI.

## Review and limits

The earlier walkthrough inspected only three initial source rows. This pass adds the required first eight rows and their field explanation. The future-noon brief and disabled-command analysis are new author artifacts; no forecast or permission-change experiment is implied. Initial read-only discovery tried two absent README paths, then located the actual lab and DATA-CARD.md through file discovery. These did not run checks or train models.

The original data plot and existing evidence-beyond-score illustration were visually inspected and preserved. Learner predictions, quizzes and teach-back remain unattempted. Original sealed files remain unchanged; this is a supplemental reconciliation, not a replacement historical run.
