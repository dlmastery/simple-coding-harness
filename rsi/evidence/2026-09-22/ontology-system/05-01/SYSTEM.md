# Give each fixed component a concrete job

The historical 05.01 run supplies the valid one-fit execution. This reconciliation checks its existing prediction artifact and supplies the omitted dry-run extension. It does not refit the baseline.

| Component | Reads | Produces | Check and limit |
|---|---|---|---|
| Task procedure and author | Bike task, one-fit allowance, constant/calendar recipe | Named run request and hypothesis | Recorded command; this is an author-guided fixed procedure, not autonomous planning |
| Domain checker | Markdown facts | DOMAIN-CHECK.md or LEAKAGE-REFUSAL.md | Three declared invariants and allowed relation names; missing facts remain a limit |
| Controller | Recipe, attempt ledger and controller contract | One admitted run and request trace | Source hash b3a398c89c673bbeccad73da1a5111caf927c0f41bb392f70eeae7f23092eb1e matches the saved contract; duplicate/budget checks are fixed |
| ML tool | Pinned data and recipe under the experiment contract | Fitted runtime object, predictions, result and ledger | Historical tool identity is recorded in CONTRACT.md; the fitted object was not serialized |
| Prediction checker | Saved predictions, source targets and row identities | Recomputed selection MAE and verdict | Original controller calls the checker after fitting; this pass also writes a separate current PREDICTION-CHECK.md |
| Reporter | Checked candidate result | RESULT.md with selection MAE 159.947912 | Refers to the same trial-001 under this workspace's contract |

The old request records attempts 0 → 1 and a checked successful candidate. Its output is evidence that the checker ran, even though the original controller did not save a separate CHECK.md. The newly written check is dated by its own command; it is not retroactively inserted into the original run.

The historical orchestration checked the leaked table and retained its refusal before the valid command. It did not mutate the components to make the leaked table pass. This establishes the declared, sequential author path; it does not prove that every possible caller is technically forced through those checks.

The diagnostic variants all reuse the same three-violation leaked table and perform zero fits. With the domain guard present, the stub is not reached. Removing that guard and leaving no other guard reaches the dry-run stub. A third, explicitly constructed column-allowlist variant still blocks total_users. That extra fixture catches an unknown column, but does not explain target derivation, final-fitted preprocessing or final-data selection.

The actual supplied ML tool also controls feature groups. These diagnostics do not bypass that implementation or prove how a different pipeline behaves. They show why removing one guard loses its particular protection, and why any claimed remaining protection needs its own check.
