# Ontology and fixed-system evidence review

The maintained driver completed four domain-check commands and one separate check of the historical system predictions. Each returned its expected status. It also executed three synthetic release-time cases and three no-training guard variants. Ten invariants pass. No model was fitted, no final score was read or computed, and no child process remains live.

The five-fact table passes. An unknown relation is refused with exit 1. The old leaked table fails three invariants. A copy containing only model uses feature total_users passes because it omits the direct derivation fact. That result demonstrates an incomplete-description limit; it is not a valid scientific experiment. The previously archived six base invariant cases and two units-extension cases remain separate evidence.

The historical system's one-fit controller hash, request record and prediction check agree. Its old command printed Checked trial-001 but did not write a separate CHECK.md; this reconciliation saves the current independent check outside the old workspace. Components were not edited during these new checks. The historical tool's recorded version differs from the current supplied tool; the new prediction check is an audit of the saved output, not a rerun with identical software.

The ten-object vocabulary distinguishes a runtime fitted object from the retained recipe and predictions. Three rewritten claims separate task-model error, skill edits and LLM weights. The ordinal wine extension and forecast impact report are plans, with no manufactured model or score.

Opened and inspected name-experiment-objects-v2.png, graph-ontology-v1.png, task-definition-availability-v1.png and fixed-components-system-v2.png. Their labels agree with the scoped lessons and the existing captions distinguish plans from measured runs. No image pixels changed. The additional Mermaid source in EXPLANATIONS.md is a declared side-by-side plan; its separate rendered appearance has not yet been inspected. The existing two-graph conceptual image was visually inspected.

These checks use one author context and constructed fixtures. They do not establish learner understanding, independent agents, real forecast data, an ordinal experiment, general ontological completeness, LLM weight changes or adversarial verification. Provider inference and author preparation costs are unknown.

The five child-command wall intervals total 7.43151030002628 seconds. The six in-process fixture timings were not separated from driver overhead. No new fitting cost was incurred.
