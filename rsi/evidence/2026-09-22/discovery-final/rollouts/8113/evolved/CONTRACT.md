# Frozen online discovery allocation

12 attempts; 120 worker-process seconds; at most 60 seconds per worker. Failed attempts count.
The policy and proposer stay fixed during this rollout. Only observations from completed attempts enter the policy view. Parent workspace copies are checked before proposal.
Policy and proposal time are reported separately. Agent inference costs are unknown. Local file access is cooperative, not a protected evaluation service.
