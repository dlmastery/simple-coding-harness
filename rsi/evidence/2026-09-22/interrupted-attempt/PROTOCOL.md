# Preserve an interrupted attempt

Declared 22 September 2026 before execution. Supplement lab 02.05's already retained orderly pause/resumption with its missing stale-running teaching case. Do not modify the historical final-locked experiment.

New allocation: one real linear/calendar bike fit, one admitted no-training interruption fixture, and two pre-fit refusal probes. The new experiment has a frozen three-attempt ceiling. The fixture consumes one slot; the real continuation consumes a second; leave the third unused. No final evaluation. One prediction check follows the real fit. No additional fits are authorized by this supplement.

A child imports the unchanged course tool. Only its fit function is replaced in that child by a marker-writing wait, so admission and lock behavior remain the actual tool's behavior. Once the parent observes the marker and confirms the child is live, terminate that exact owned child. Retain its terminal return code, PID, lock bytes and running ledger. This is a real process termination before model training, not a training checkpoint-resumption test.

Probe continuation while the stale lock remains: expect refusal before admission. Confirm the owned child is terminal, archive and remove only its stale lock. Probe again with the unresolved running record: expect refusal before admission. Preserve the original ledger, mark its existing row interrupted, leave its slot charged and record known elapsed time separately. Resume one real distinct recipe in a new process; require trial-002, preserve trial-001, and check the saved predictions. Never refund the interrupted slot or reuse its identity.

Use 60-second command limits and a bounded readiness wait. Stop and preserve any unexpected outcome. Exact source hashes, child code, commands, state snapshots, check results and timing belong in the archive. Learner, fresh-agent, in-training optimizer recovery, native remote backends and total inference costs remain untested.
