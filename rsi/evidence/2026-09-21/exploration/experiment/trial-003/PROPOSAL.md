# trial-003

Adding the permitted weather group to the calendar Ridge model will reduce selection MAE compared with linear/calendar; the test keeps model family, seed, split, metric, and estimator settings fixed.

Model: linear. Features: all. Seed: 17.

Policy snapshot records provenance; it does not prove the agent followed that policy.
