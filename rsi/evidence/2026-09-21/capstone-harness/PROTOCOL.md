# Build and check the new capstone harness

Declared 21 September 2026 before any capstone fit. Lab 11.01 permits at most four small CPU fits. This walkthrough allocates two: one median baseline in the generated package and one in a clean exported copy. Each experiment receives a one-attempt contract. The other two fits remain unused. No remote or paid compute is authorized or needed.

The new task is white-wine quality regression using the already supplied UCI table. Earlier wine labs classified red wine at a threshold. Here the target is the original ordinal quality score and the metric is MAE. Predictions remain continuous. This is a retrospective benchmark after laboratory measurement, not a causal, commercial, or future-vintage claim.

Group identical input vectors before splitting. Use one deterministic feature-only hash, with fixed 60/20/20 hash ranges for train, selection, and evaluation. Do not search split seeds. Fit preprocessing only on training. Final evaluation remains unused during this lab and publicly accessible on the host; the package enforces procedural access, not evaluator secrecy.

Generate the package through the current build-ml-harness skill. Retain the exact builder identity, task, data card, workflow, implementation, evaluator, tests, dependencies, recovery guide, and provenance. The shared bike/wine runner does not express this new regression target, so the coding agent generates a separate bounded runner.

Execute one real baseline, independently recompute selection MAE from predictions, and test meaningful invalid requests: target-as-feature, altered metric, exhausted budget, and a falsified report copy. A malformed request is refused before fit admission. Any admitted failure retains its charged attempt. One separate labelled fit-failure stub may test that accounting without training a model; charge its simulated attempt and do not count it as an actual fit. Test the same baseline from an exported copy without changing the scientific contract. Compare predictions and record environment limitations.

Prepare a larger-job plan through scale-experiment. Keep it generated-only because no target cluster is supplied. State resources, cost ceiling, cancellation, retry, and checkpoint policy; do not invent hardware access or backend execution.

Keep all commands and exit statuses. Stop after the two allocated fits. Learner predictions, quizzes, and peer reproduction are unattempted. The author has seen the dataset family and earlier results; this is a course verification, not blind autonomous task discovery.
