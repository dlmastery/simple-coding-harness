# Continue from the verified package

Lab 11.01's two allocated fits are complete. Each one-attempt experiment is exhausted. Do not reopen either experiment to run a new candidate. The remaining lab ceiling is not an automatic authorization for extra search; this protocol stopped after two fits.

Start a future student session by giving the coding agent package/README.md and package/TASK.md. The agent installs requirements, prepares a separately allocated sibling workspace, runs its baseline, and checks predictions. The saved command records show the exact author invocations; regenerate paths for the student's machine.

For lab 11.02, reuse the frozen package and write a new two-generation, eight-fit-total protocol before execution. The solver and improver are different objects. The package has no final-evaluation command yet; generate and freeze that separate evaluator under the next protocol before its fits. Preserve this package as the baseline and document any necessary extension.

Both current runs use Python 3.12.12 on Windows 11, numpy 2.5.3, pandas 2.3.2, and scikit-learn 1.7.2. Exact installed dependencies appear in the command records. The exported environment was newly installed; package code, task, data, and prediction hashes match. This does not test another native coding agent, another operating system, or a peer.

Source data and baseline predictions are public. Evaluation is unused in this lab, but not technically hidden from the author. Do not claim blind transfer, causal learning, or protected external assessment.

No running process remains after the recorded export completed. A generated larger-job plan is in package/SCALE-PLAN.md. Remote hardware, scheduler, cancellation, and resume behavior are not tested.
