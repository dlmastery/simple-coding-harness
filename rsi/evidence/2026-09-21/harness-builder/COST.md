# Resource record

Two matched executions each contain one fit, the generated controller's internal prediction check, a separate prediction-check process, and an explicit-recall report gate. No extra candidate or final evaluation ran. Four subprocesses succeeded.

Cumulative subprocess seconds: 
6.266259400000001
Cumulative recorded fit-related seconds: 
0.099396
Fit-related time is nested in subprocess time; do not add it twice.

Source reading, historical Git recovery, author diagnosis, implementation, inference, and publication costs were not measured. The failed parent report gate remains in the evidence. A read-only git diff returned exit 1 because the files differ; this is the expected diff status, not an experiment failure.
