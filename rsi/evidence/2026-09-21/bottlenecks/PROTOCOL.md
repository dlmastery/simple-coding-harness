# Bottlenecks author protocol

Lab 10.38 executes exactly five synthetic timing scenarios and audits existing local evidence. No new model fit, paid compute job, or paper-model simulation. This maintainer check is independent of the still-pending six-system matrix in lab 10.37; it does not claim that prerequisite activity is complete.

Use minutes and sequential stages. Total time equals proposal plus execution plus evaluation plus additional checking. Execution is zero only for this teaching calculation. The five fixed inputs are: baseline (1, 0, 9, 0); faster proposal (0.5, 0, 9, 0); faster evaluation (1, 0, 4.5, 0); added checking (0.5, 0, 9, 1); costlier verifier (0.5, 0, 12, 0). Compare each independently with baseline, not as successive generations. The fifth is the lab's additional-change extension. The instantaneous-proposal bound is an algebraic limit of baseline, not a sixth execution.

Also reproduce the illustration's separate arithmetic: increments 4, 2, 1 yield cumulative totals 4, 6, 7. These are unspecified synthetic gain units, not performance or measured research outcomes. They make the distinction between cumulative and marginal gain visible without attributing a research forecast to either paper.

Read Economics of Recursive Self-Improvement's model assumptions from the version-1 PDF, since the HTML exposes mainly its introduction. Read the optional Recursive Criticality sections 2–3. Record exactly what was inspected. Do not infer empirical acceleration from either a model threshold or this calculator.

Copy the existing two-generation lineage, result, state, and cost files. Recompute retained-task improvement and recorded fit-process wall time for each path/generation. Call that denominator fit-process wall time, not total research cost. Inference, design, proposal, and other unmeasured work remain unknown. Do not add nested fit seconds to enclosing process seconds. Include both rejected improver proposals in interpretation.

Preserve source hashes, calculator source, inputs, outputs, arithmetic assertions, and the acceleration audit. Learner prediction, quiz, and teach-back remain unattempted. No worker or training process is launched.
