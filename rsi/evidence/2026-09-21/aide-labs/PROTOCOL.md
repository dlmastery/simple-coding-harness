# Inner search, outer comparison, and a separate role test

Declared 21 September 2026 before this run. Use a new sibling workspace, rsi-work-2026-09-21-aide-labs. This is an author walkthrough with a fixed controller, readable procedures, and genuine small regression fits. It is not autonomous LLM research or a reproduction of AIDE2. Learner predictions, teach-back, and quizzes are untested. The author has seen earlier bike results; these are exposed development data.

## 10.13: four fits

Keep the supplied bike data, evaluator, seed 17, and tool fixed. Inspect the data first. Retain all attempts and independent prediction checks. The unchanged researcher R0 reads four ordered operators: draft, refine, explore, enrich. Draft fits the training-median baseline with calendar inputs. Refine changes the retained parent's model to linear, preserving its inputs. Explore changes it to a tree, preserving inputs. Enrich adds permitted weather inputs to the retained parent's model. Before every fit, record the operator, parent, proposed recipe, procedure hash, and remaining budget. Lower selection MAE wins; a tie keeps the earlier candidate. A failed attempt is not refunded; stop for inspection.

Save the selected recipe and the complete search trace. Replay the order draft, refine, enrich, explore using only recorded recipe outcomes. Stop with unknown when that order asks for an unobserved recipe. This is a deterministic recipe replay, not a stateful-agent counterfactual. No additional fits and no final evaluation.

Pause after this trace. The actor reads it and records a proposed change before the next lab. A weak result is a search failure to improve, not necessarily a tool failure.

## 10.14: six fits in a separate matched experiment

Use R0 and one proposed child R1. Preserve the proposal's reason and freeze both procedures before either comparison arm. Each arm starts empty, receives three fits, and uses the same data, evaluator, seed, parent selection, and retention. The earlier four-fit score is not the parent arm. The allowed change is operator order. Retain both arms even when the child loses. Compare retained selection MAE and all recorded fit and subprocess costs; unavailable author/inference cost is unknown. Do not access final data or call this a new independent test.

Prepare a separate unequal-budget illustration with parent allowance three and child allowance six. Do not execute those extra fits or invent their scores. Its purpose is to expose the changed comparison, not to increase this lab's budget.

## 10.15: two mechanical outer proposals and matched decisions

Freeze the role adapter before producing either proposal. It applies each frozen researcher's third operator preference to the third slot of the same target T0 (draft, refine, explore). This deliberately narrow projection makes the producer-to-proposal link executable. It is not an LLM authoring a new researcher and cannot measure general improvement ability.

Each proposer gets one projection and each proposed target gets the same three constructed fixtures, with zero fits. In a fixture, the retained recipe is linear/calendar. Case A has already tried tree/calendar: an explore proposal repeats a recipe, while enrich can propose linear/all. Case B has already tried linear/all: enrich repeats a recipe, while explore can propose tree/calendar. Case C has no remaining attempt: both must stop. The fixed outcome rule rewards a valid unseen proposal when an attempt remains, or a correct stop when none remains. Reject duplicates; do not repair them. Cases A and B deliberately expose opposite weaknesses. No fixture has a task-quality score.

The local result can test scheduling behavior and expose role-dependent limits. It cannot establish ignition, statistical superiority, or model-quality gains. Keep an additional overcomplicated-proposer example separate: a five-child expansion with one remaining slot must be refused by the budget check. One extra structural check, zero fits. Do not transfer the earlier MAE into the outer outcome table.

## Accounting and publication

Ten model fits across the first two activities. Two proposal projections, six target decision fixtures, and one separate overcomplication check in the third. Record source hashes and freeze checks. Retain the original workspace, all scripts, errors, checks, and exact-byte archive. No new generated infographics: use the existing reviewed illustrations. Label any new result table as measured, constructed, or unknown.
