# Visual and path review

All three first-draft PNGs were inspected. Revision 2 shortens one connector label and wraps the pinned-data path so it stays inside its box. The selected data-flow figure was inspected after rendering. No image-generation calls, fits, or workflow checks were made.

After rendering, corrected VIEWS.md to state the proper base directory for workspace-relative paths. Its prior text and original manifest are preserved. The archived build-three-views.py is the exact executed renderer; the maintained source contains this prose correction for a future fresh generation. The final manifest below accounts for the correction and retained drafts.
