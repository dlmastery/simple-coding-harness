# A real score can be ineligible for a comparison

The first browser attempt selected A and failed the fixed check. The second opened A's and B's details, selected B, and passed. Both used the same frozen page and real saved scores. No model was fitted.

| Row | Saved MAE | Meaning in this task |
|---|---|---|
| A: screened forest | 73.688599022211 | January 2012 selection after January–March 2011 training; ineligible for the required full comparison |
| B: fuller forest | 98.03122050618987 | Full 2011 training and January–June 2012 selection; eligible and best of the eligible rows |
| C: fuller linear | 99.17592378687597 | Same full scope; eligible but higher MAE |

The values come from the copied source result and trial ledger. Display rounds them to six decimal places; the checker uses saved precision. A is not a fraudulent or leaked model: it is a valid screen result used outside its comparison scope. This is the live exercise's counterpart to the illustration's conceptual excluded-input warning.

The first skill deliberately omitted detail inspection. Its selection is an executed negative control, not an accidental or independently discovered agent failure. The critic packet contains the task, visible accessibility states, action records, and the first selection screenshot. It does not contain detail contents or the answer key. The current author nevertheless knows all conditions, so the critic is not isolated. Its packet-limited conclusion was unverified; the separate executable check establishes failure.

The second skill changes one selection instruction and clarifies its explanatory comment. The author reads and follows the skill; it is not a separate automated skill-loading agent. The server snapshots the file at each attempt's start. The initial second-attempt table shows C's score above B, so filtering C out does not hide a lower score in this three-row task. The actor then inspected both forest rows. The saved warning and eligible-detail observations precede selection B.

Both attempts were actual browser interactions through the Codex in-app browser. The server records Start, Apply filter, Details, and Select events. It does not log input keystrokes, viewport scrolling, screenshot capture, or every accessibility read. Saved visible states and screenshots complement that action log. The page was reloaded once for attempt two; no third attempt ran. The temporary tab was closed and the server stopped after the second check.

An optional full-page screenshot call failed because the named browser variable was not bound; it failed before any page action. The original viewport capture remains. Scrolling the completed result then captured the selection status. This was documentation of the second attempt, not another selection. The warning screenshot and final status screenshot were visually inspected. No generated infographic was changed or regenerated.

The fixed checker reads the recorded UI selection and picks the minimum score among rows with the required scope. It is independent of the actor's selection command, but was written by the same author. The loopback server and hash guards are local process controls, not adversarial evaluator secrecy. No broad transfer, independent-context comparison, source benchmark reproduction, or training claim follows.
