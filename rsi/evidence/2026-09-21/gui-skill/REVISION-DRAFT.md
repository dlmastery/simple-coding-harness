# One instruction changed

The exact checker rejected A: only the full-scope candidates can win, and B has the lower MAE of those two. The critic's packet-limited verdict was unverified, not an invented factual claim that it saw the hidden warning.

SKILL-v2 changes instruction 2 to require detail inspection before selection and to check whether filtering removed a lower-scoring row. Instruction 1 remains unchanged. The legacy final paragraph is retained byte-for-byte to keep this a one-instruction comparison; it describes the deliberately deficient v1 control and is stale in v2. It is a documented presentation limitation, not an executable instruction. A student-facing reusable skill should remove that obsolete comment after this two-attempt study.

The controller snapshots the actual skill file at the start of each attempt. No third attempt is permitted. The unchanged page already contains the scope warning; no warning is added after the failure.
