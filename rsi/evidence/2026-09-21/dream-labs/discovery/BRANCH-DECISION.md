# Two branches after the baseline

The measured training-median baseline has selection MAE 159.947912. Its fixed prediction cannot respond to the supplied calendar and weather inputs. Within the already declared two remaining fit slots, compare a linear model and a depth-limited tree using the same all-input group, split, metric, and seed.

B tests an additive linear response with encoded calendar categories. C tests nonlinear splits and interactions. Both proposals use A as the conceptual baseline. They do not resume A's filesystem in separate agent workers and do not constitute the source paper's leaf-eligibility transition rule.

The author has prior exposure to this bike task. These branch choices were planned before the baseline; this note records their motivation after inspecting A, not autonomous discovery. No B or C result has been produced in this experiment yet. Keep both measured outcomes even if they fail to beat A.
