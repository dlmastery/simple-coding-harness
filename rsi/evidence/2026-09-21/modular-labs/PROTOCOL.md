# Local repair, integration, and lineage

Declared 21 September 2026 before the fixture executions. New sibling workspace: rsi-work-2026-09-21-modular-labs. Zero model fits across all three activities. These are deterministic author-constructed interfaces, not independent coding agents or a source-paper reproduction.

## 10.10: locate one missing field

Prepare two prerequisite traces under H0: a short observation and a long observation for the same candidate. The context component erroneously drops candidate identity when it compresses a long details field. The fixed completion gate requires candidate identity. Execute both fixture captures and retain the passing and refused traces. These are two prerequisite executions, separate from the lab's two patch checks.

Pause for the actor to inspect both traces, record the earliest divergence, and write one context-only patch. Keep loop, tool, observation, and completion files byte-identical. Run exactly two patch checks on the same cases. Use a fit stub that only records whether it was reached; never train a model. Preserve component hashes, before/after contexts, gate results, and source versions. A second-component change stays an unexecuted proposal.

## 10.11: integration is a separate question

Declare four versions before execution: base, A only, B only, and A+B. Base context keeps explicit duration_value and duration_unit. A normalizes to duration_seconds and removes the explicit unit field. Base completion understands either representation. B adds a required explicit duration_unit check, accepting seconds or minutes, while leaving base context unchanged. A and B can each pass alone. Together they conflict because B still requires the field A removes.

Use a predeclared original case (2 seconds), a fresh fixture (0.5 minutes), and a malformed fixture missing candidate identity. Fresh means not used to choose or edit the components; the author still designed and can read it. Four versions run on the original case, then base and combined run on the fresh case. The seventh execution runs combined on the malformed input. It must refuse before the fit stub. Seven fixture executions total, zero fits. An individually passing edit need not compose with another; retain the rejected combination and do not repair/retest it within this allocation.

Before execution, save produced/consumed fields, units, possible conflict, expected verdicts, and all version hashes. Validate seconds conversion against expected values on accepted paths. Do not treat an expected refusal as a passing integrated harness; report both the harness result and whether the check observed the expected outcome.

## 10.12: inspect source mechanisms and actual ancestry

Read DGM and HyperAgents primary method descriptions and associated source diagrams, preserving access limitations. These are explicitly dated older foundations, not current-month discoveries. Record mutable surfaces, the producer of each change, parent selection, evaluation, and inheritance. Keep claims within selected-source reading.

Audit the existing two-generations record with zero new executions: actual improver/task files, their hashes, promotion decisions, and the generation-two before-action trace. Make a typed edge table and a separate copy without the improver identity. Explain what that removed column prevents a reader from deciding. Do not convert the run's two rejected improver proposals into accepted revisions. Preserve relevant source-record copies and exact hashes.
