# An unexecuted two-component alternative

Change context.py to retain candidate identity, and also change observation.py to shorten details before context runs.

Both changes address the same long-observation trigger. If the repaired system then passes, the outcome alone cannot tell whether the context fix was necessary: upstream shortening could prevent the faulty branch from running. Separate edits and contrasts would be needed to attribute their effects.

This proposal is explanatory only. No second component was changed and no additional fixture execution was allocated.
