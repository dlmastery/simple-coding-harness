# A bounded lesson from exploration

Author: the coding agent acting as the task actor, after reading the outcome check.

On the retained bike-demand selection split, Ridge with calendar inputs had MAE 109.807668. Adding the permitted weather group produced MAE 99.175924. The independent calculation checked row identities, targets, and the reported score. It did not approve this memory statement.

Before one bounded feature probe, inspect selection residuals against permitted inputs that the current recipe omits. Use the same split and metric for the comparison. Save the reason before fitting, count the fit, and retain the result even if it is worse.

Scope: a suggestion for tabular regression experiments with a fixed selection split and an explicit feature allowance. A residual association can motivate a test; it does not establish a cause. The observed improvement belongs to this dataset, split, model, and feature change. It does not mean that linear models, weather features, or this procedure always win. It does not establish recursive self-improvement.

Source: rsi/evidence/2026-09-21/exploration/README.md, trials 002 and 003. The verifier checked trial-003 again in this activity; the actor interpreted the comparison.

## Lesson from the frozen comparison

Both paths used the same residual rule and chose the same feature group. Both obtained evaluation MAE 7.027189070. The memory file stayed unchanged, but the author had seen it when designing both paths. This run therefore demonstrates file freezing and matched fit budgets; it does not isolate a memory effect. Use separately initialized agent contexts and declared information access for that stronger comparison. This copy update has not been tested for a performance benefit.
