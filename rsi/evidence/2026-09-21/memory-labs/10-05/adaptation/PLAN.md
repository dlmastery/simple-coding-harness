# Separate adaptation-copy activity

The frozen comparison has finished. Allow one actor-written update to a separate copy of its memory, with zero extra fits. Keep a before copy and hash both copies. The original 10-04/MEMORY.md must still match the comparison freeze. Add the lesson about shared context and identical choices; do not invent a measured adaptation benefit.
