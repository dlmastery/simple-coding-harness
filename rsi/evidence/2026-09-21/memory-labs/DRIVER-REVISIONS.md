# Driver snapshots

run-memory-labs.py is the exact driver retained before the outcome check. Before the probes and follow-up phases, run-memory-labs-v2.py added a check that each recorded actor decision keeps its SHA-256 through its follow-up fit. The model, data, thresholds, and budget were unchanged. All later phases use v2. Both source versions are retained.
