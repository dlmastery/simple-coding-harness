# Inspect or repeat this experiment

Start with PROTOCOL.md, LAB-NOTE.md and CLAIM-AUDIT.md. Read I0.md, CHANGE-PROPOSAL.md and I1.md in that order. Then compare the two generation-two START.md files and TRACE.csv files. PROMOTION.csv is the pre-final decision. PRE-FINAL.csv identifies the exact artifacts frozen before FINAL-RESULTS.csv was created. COST.md distinguishes fitting intervals from total unknown research cost.

The archive includes the generated package, original CSV, eight fitted models, predictions and every command log. The runtime is recorded in commands/00-environment.md. A coding agent can recompute predictions and scores without training; the supplied audit.py imports the local package and validates these identities. Its output files use exclusive creation to avoid overwriting evidence, so run a repeat audit against a copy with a distinct output path chosen by the agent. Do not delete or replace the sealed audit outputs.

To perform a new replication, ask the tutor for a separately labelled experiment with the same protocol and budget. The maintained author driver is run-capstone-recursion.mjs in the repository's provenance scripts. Its phases are prepare, generation-one, comparison, final and seal. Stop between the first generation and comparison to inspect results and save an actual proposal. Its current named workspace deliberately refuses an existing run; an agent must prepare a new labelled copy and document all path changes. The driver does not invent a favorable result or proposal.

The package was extended before fitting: run.py saves model.joblib and training predictions; recursive_control.py reads Markdown policies, records selection, freezes the external decision and performs one terminal evaluation. task.py includes that controller in its package identity. The earlier 11.01 package is unchanged. The copied baseline package documentation describes that earlier package; this capstone's PROTOCOL.md and EXTENSION.md define the additions.

Dataset permissions, source and grouped split are inherited from the package's data card. Matching feature rows remain in one partition. No producer or time identity is available, so grouping inputs does not remove every possible dependence. These public local files do not create a private evaluator service.

All figures remain in the course assets. Use the 11.02 concept figure together with the measured table; do not read its arrows as proof of a later post-acceptance round. No such round ran.
