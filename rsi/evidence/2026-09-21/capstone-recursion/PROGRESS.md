# Closed experiment

Two generations complete. Eight of eight actual CPU fits charged; zero remain. The external gate accepted I1 before terminal evaluation. Both frozen retained models were evaluated once. The extra-fit, repeated-final and post-final-fit requests were refused. Forty-three post-run checks passed without fitting.

Do not remove FINAL-LOCK.md, rerun either phase, add a third generation, or revise a decision after final exposure. Inspect archived outputs to reproduce the analysis without training. A separately declared replication needs a new labelled experiment and its own budget; it cannot turn these exposed cases into untouched evidence.

No process remains active. There were no failed fitting attempts. Rejected recipes remain in the trace. Generated Python caches are omitted from publication; generated source, data, models, predictions, Markdown skills, protocols, commands and audit are retained.

Next: test task and host portability in 11.03, then prepare the research audit and final teaching portfolio. Do not claim the later capstones are complete from this result.
