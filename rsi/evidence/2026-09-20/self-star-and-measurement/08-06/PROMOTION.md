# Reject and restore

The fixture's majority classifier has accuracy 0.871473354, negative recall 1.0, positive recall 0.0, and balanced accuracy 0.5. The primary metric remains balanced accuracy. Reject the proposed metric switch and restore the exact prior active-file bytes. Both instruction versions and the invalid intermediate pointer remain. No fit was needed.
