# Synthetic graph fixtures

Effective repair: [(0, 'missing target'), (1, 'valid')]

Exhausted repair: [(0, 'missing target'), (1, 'missing target'), (2, 'missing target')]

These are constructed control-flow checks, not ML performance results.
