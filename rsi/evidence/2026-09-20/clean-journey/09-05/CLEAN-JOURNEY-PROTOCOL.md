# Clean-workspace learning-path walkthrough

Protocol recorded on 20 September 2026, before the activity runs.

## Starting boundary

Clone the published branch into a new directory and install the documented dependencies in a new project-local environment. The actual starting commit is `75e998848730c268849f67514e249a186bad86da`. The new clone is `work/clean-journey-75e9988/course`; generated learner work belongs in its sibling `rsi-work` directory. Preserve the original authoring checkout.

The clone and dependency installation succeeded. Python is 3.12.12. The preceding dedicated RSI workflow also passed on all three hosted operating systems at this exact commit: [run 35507391379](https://github.com/dlmastery/simple-coding-harness/actions/runs/35507391379).

The maintainer reads the student entry point and canonical skills, then writes and executes the implementation that the lesson requests. This is one authoring-agent context. New Python processes and new filesystem state do not create fresh agent contexts. Prediction, quiz, and teach-back responses remain unattempted. Do not manufacture a learner transcript.

## Route and evidence

1. Frame the bike task, inspect actual rows and splits, fit a baseline, and verify its predictions.
2. Save and reuse the fixed procedure. Retain a real row-identity failure and a reproducibility comparison.
3. Run controlled model and feature changes. Generate a bounded controller; exercise duplicate refusal and resume across process boundaries.
4. Exercise dependencies, routing, joins, bounded repair, selective recovery, and domain rules on the artifacts just produced.
5. Generate and execute bike and wine harnesses from readable briefs, with their contracts, meaningful checks, and recovery instructions.
6. Trace solver, research-skill, and improver versions separately. Run a matched, explicitly bounded comparison and state the limits of the available context boundary.
7. Freeze a retained model, perform final evaluation once, and audit the strongest supported claim.

Record exact activity coverage and omissions rather than treating this selected route as 101 completed labs. Existing component tests and earlier simulations remain separate evidence. Optional activities are included only when explicitly recorded and budgeted.

## Execution rules

Use one workspace per lab. Declare each lab's fit limit before execution. Each child command has a 60-second timeout and retained exit status, standard output, standard error, and elapsed wall time. Keep failed and refused requests. Do not execute concurrent writes to a workspace. Apply the narrower lesson budget and preserve attempts across restart.

Reuse fixed input artifacts only when their hashes and contract match. A deliberately altered copy must remain separate from the original. Never select on final outcomes. The public data remains visible to the authoring agent; no secret evaluation boundary is claimed.

No unrequested agent subworkers, paid cluster runs, or native-agent compatibility claims. Model-fit time and command wall time are measured separately; hosted inference costs remain unknown. Stop on an unexpected failure, save the state, fix the responsible instruction or tool, and retain the failed attempt.

## Publication

Keep the generated work in the sibling workspace during execution. Publish a byte-preserving copy of suitable run artifacts under `rsi/evidence/2026-09-20/clean-journey/`, with hashes and source-commit identity. The clone and installed dependencies are reconstructible inputs, not duplicated course artifacts. Retain the authored drivers under this provenance directory. Push coherent milestones and verify the remote hash.
