# Verify coordination before actions

Declared on 20 September 2026 after reviewing the thirteen-fit loop/system driver. That driver fits and checks its 05.04 baseline, then constructs a transition table. Its fixture verdicts are executed, but the table is a retrospective reconstruction. It does not prove that saved state governed execution. Preserve that run and its cost.

Run a separate one-fit experiment to test this missing property. Do not reopen or increase the old workspace budget. Use the same fixed bike constant/calendar recipe, seed 17, and the supplied checker. This is a control-flow repair, not an attempt to select a more favorable model result.

## Required actions

1. Create a new sibling workspace and save the coordinator skill. Record ready before executing anything. Target identity and one-fit allowance are explicit.
2. Read current state from disk. Admit a fit only from ready. Save running before launching the child command. Preserve its command, output, exit, and wall time. A timeout or failure records needs-review and stops; no automatic retry.
3. After the successful fit, save awaiting-check. Persist its full workspace-qualified candidate identity and prediction digest. Exit this process before checking.
4. In a new process, read the saved state. Test missing-check, ambiguous-target, and wrong-candidate handoffs as separate fixtures without changing the active run. The wrong-candidate packet must refer to a genuinely checked earlier baseline. The matching prediction bytes make this an identity check, not a metric check.
5. Run the actual checker. Save complete only when task, candidate, prediction digest, and successful check agree. Preserve event order and each state snapshot.
6. Invoke the fit action again. It must refuse without fitting. Record its expected nonzero exit. This is a controller refusal, not a new candidate attempt.

Budget: one new fit, one real result check, the three specified handoff fixtures, and one post-completion refusal. Each child has a 60-second timeout. Parent state transitions are flushed before actions. No final evaluation, independent agent, learner response, or GPU run is involved. Cumulative new fits across the original walkthrough and this correction will be fourteen, if all planned fits complete. No cost from the original run is discarded.
