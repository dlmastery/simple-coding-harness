# Lab 07.03 brief

A versioned memory note that changes a later decision.

Starting state: A tested reflection, a new case, and a fresh session where available.

Prediction to ask: What would demonstrate use of the memory rather than mere file existence?

Execution limit: One later task or fit; preserve a no-memory comparison if already available. The additional scope check uses one separately labelled fixture and no extra fit.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The memory version is identified. A later action is linked to a specific rule. The result distinguishes use from benefit.
