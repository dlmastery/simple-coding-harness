# Zero to Hero: Recursive Self-Improvement — a hello world, skills only (outline, v4)

**The skill pack gets smarter. The model weights do not. You can measure both, turn it off, and roll it back.**

Hello world, concept first, **skills-based from the first step**: the agent is
configured by files it boots (`SKILL.md` and its siblings), never by Python
that drives the loop. Python only supplies the *tools* a skill names
(`fit_recipe`, `write_card`, `score_test` …) and one tiny skills harness that
boots a pack and runs the model loop — the same shape as the root codelab's
stage 4 (skills) + stage 15 (`execute()` as the one place every tool call goes
through). Every step adds one idea by changing the files, so `diff -r` between
two steps shows exactly what became RSI. Every step runs offline in tests with
a fake model; `python run.py` needs `BASE_URL` / `API_KEY` / `MODEL` like the
rest of the repo.

## The job (real software, no toy factory)

Improve a tabular classifier on **Adult Census Income** under a hard budget of
**24 `fit()` calls per arm**, then prove the *pack* improved, not just the
model: freeze the memory and run the same budget on a **shifted synthetic
table** it never saw. Bundled 6k-row Adult sample (offline), OpenML cache when
online. Models: `LogisticRegression`, `RandomForest`, `HistGradientBoosting`.
Metric: ROC-AUC on validation during search; the test split is scored **once**,
after freeze, by a tool that raises on a second call.

## The definition we use — *The Last AI Built by Humans* (arXiv:2609.11873)

RSI: "an autonomous, closed-loop process in which an AI system identifies its
own limitations, develops and validates improvements, and uses the resulting
capabilities to improve the improvement process itself." Persistent changes
across rounds that affect how later improvements are generated, evaluated,
selected or consolidated. Not RSI: B0 ("output change without persistent
system change"), AutoML / continual learning with designer-fixed objective,
search space and acceptance test. **Structural** recursion (a revised mechanism
governs a later round) vs **effective** recursion (stronger successors under
comparable budgets and independent evaluation). Three problems every claim
must answer: **safe inheritance** (transfer tests, version histories,
rollback), **autonomy attribution** (AI-controlled decisions vs fixed
procedure vs human acceptance), **reliable verification** (protected
evaluation, matched budgets). Rungs: L1 execute → L2 choose strategy → L3
choose experience → L4 revise state under an external acceptance rule → L5
revise the improver itself. Every step names its rung and what stays human.

## The one harness (built once, in `rsi/common/`, ~200 lines, never a step)

`harness.py` — `boot(pack_dir)` reads `SKILL.md` (becomes the system prompt),
`tools.md` (which tools this pack may call — the allowed set), `schema.json`
(the recipe space, given to the model verbatim) and, when present,
`memory.json` (cards, appended to the prompt unless `MEMORY_OFF`). Then the
loop from stage 15: call the model, run every tool call through `execute()`
(unknown / disallowed / malformed → `Error:` result, never a crash), append,
repeat, stop when the pack's budget is spent or the model answers in text.
`tools.py` — the tool table: `load_splits`, `fit_recipe(recipe)`,
`read_memory`, `write_card(card)` (verifier only), `read_traces`,
`score_test(recipe)` (once, after `FREEZE`), `rank_policies(names)` (step 03),
`private_score`, `patch_pack`, `rollback` (step 04). Every tool is a gate:
`fit_recipe` counts the budget and refuses a recipe a `forbid` card rules out;
`score_test` refuses before freeze and on a second call; `write_card` refuses a
card that names test or intent. The skill *says* the rule; the tool *enforces*
it — that is what makes a skill pack testable. `fake.py` — a scripted /
rule-driven fake model for the tests (reads the cards in its prompt and
proposes accordingly), so every claim is provable without a key.

## Steps — each is a skill pack plus at most one new tool

The sequence follows the owner's rough plan (regular → loop engineering →
graph engineering → RSI inner pack → meta pack): two rungs of *harness
engineering that is not RSI* before the first file that is. Pack files use
the repo's skill front matter (`name`, `description`, `metadata: {type,
version, rsi}`), so the same packs load in the root codelab's harness.

| Step | The pack (what changed on disk) | The one idea | Rung |
|-----:|-----|------|------|
| 00 `regular_skill` | `skills/adult-income-regular/`: `SKILL.md` (boot only this pack; fit the baseline; run the static 24-recipe list in order; pick best val; `score_test` once; `save_model`; stop; "do not write memory.json, do not edit this file, next run boots this same text"), `tools.md` (allowed: `load_splits`, `fit_recipe`, `score_test`, `save_model`; forbidden: `write_card`, `read_memory`, edits), `schema.json` (task, target, metric, `n_fits: 24`, the baseline recipe, `search_policy: static_list`, the 24 recipes) | A repeatable trainer. Same text every run, same waste every Monday. **The control arm, and not RSI** — say so. The harness, the budget, the locked test and the append-only trace arrive here. | B0 / AutoML (deliberately) |
| 01 `loop_skill` | `skills/adult-income-loop/`: `SKILL.md` ("this pack engineers the inner loop only; the loop text does not rewrite itself"), `loop.json` (`kind: counted_while`, `N: 24`, counter `t`, `error_still_counts: true`, `body`, `exit: FREEZE → score_test once → save_model → stop`, `illegal: change N, reorder, second while, score_test before FREEZE, write memory.json`), `recipes.json` (baseline + `static_list` 1..24), `tools.md` adds `write_loop_log` (audit only) | **Loop engineering**: name the steps, the counter, the gate. The tools enforce what `loop.json` declares (`fit_recipe` refuses the 25th, `score_test` refuses before FREEZE). Still not RSI: the log is audit, never fed back; generation n+1 loads the same `loop.json`. | harness engineering, L1 at most |
| 02 `graph_skill` | `skills/adult-income-graph/`: `SKILL.md` ("the training job is a DAG, not a free-form script"), `graph.json` (nodes = legal operators `load`, `encode_*`, `scale_*`, `model_*`, `score_val`, `pick_best`, `score_test` with `gate: freeze_only`; edges = hard dependencies; constraints: one encode, one scale, one model per path, no cycles), `paths.json` (baseline + 24 static paths with parameter bindings), `tools.md` adds `walk_path(path_id)`; illegal path = skip, count as error, never invent a substitute | **Graph engineering**: a recipe is a path, `walk_path` fires the nodes in edge order, `score_test` is a sink reachable only after FREEZE. Still not RSI: `graph.json` and `paths.json` are `mutable: false`; the walk log is audit only. | harness engineering |
| 03 `memory_skill` | `skills/adult-income/` (inner H): `SKILL.md` (boot order: this file, `tools.md` + `schema.json`, `memory.json` unless `MEMORY_OFF`, `traces.jsonl` append-only; for t in 1..24 read cards, propose ONE recipe from the schema, fit, append the trace; FREEZE; `score_test` once; "only adult-income-meta may patch this pack"), `memory.json` (starts `[]`), `memory.schema.json` (`if` profile predicate → `then` prefer/forbid one field value, `evidence`, `counter`), `tools.md` adds `read_memory`; a second pack `skills/adult-income-verifier/` (`SKILL.md`: input is `{recipe, val_auc, error, profile}` only, IF/THEN cards with evidence and counterexample, refuse cards that mention test or intent; `tools.md`: `read_traces`, `write_card`) | **The first RSI file.** The card is a constraint on the *next* proposal, not a diary; `fit_recipe` refuses a recipe a `forbid` card rules out; no one grades their own homework — the verifier pack boots with only the log. `MEMORY_OFF` is the off switch: same 24 fits, the numbers must fall back. | L4: deployment feedback revises persistent state; the acceptance rule stays ours |
| 04 `proof` | `eval.md` in the inner pack (24 vs 24 with `MEMORY_OFF`, the delete-file check, the scorecard fields, "test touched before freeze: must be no"); `run.py --transfer` boots the same frozen pack on the shifted synthetic table | The locked test proves you did not peek; the shifted table proves the *pack* got better — effective recursion. The scorecard is the deliverable. | evidence standard: matched budget, protected evaluation, transfer |
| 05 `meta_skill` | `skills/adult-income-meta/` (MH): `SKILL.md` ("you do not fit models; you edit the inner pack"; needs ≥ 8 new trace rows; write atomically at most one of: new cards, a small `schema.json` patch, a small patch to the inner **search policy** line; hard rules: no `score_test`, no new tools, no weights, size cap 20 %, one module per visit), `tools.md` (`read_traces`, `read_memory`, `read_pack`, `rank_policies`, `patch_pack`, `private_score`, `rollback`), `policies.md` (random, neighbours-of-top-3, broad-then-deep, obey-memory); `versions/` keeps the inner pack per generation; `run.py` boots inner → meta → inner | Generation n+1 is RSI only if it boots what generation n wrote — the reboot is the recursion. `rank_policies` scores policies on the log with zero fits (Dream-RSI, bounded: unvisited = unknown). `private_score` (a split the inner pack never sees) decides keep-or-rollback: reliable verification and safe inheritance made concrete. `META_OFF` turns the outer loop off. | L5 flavour: the improver's policy line is revised by the system; the archive rule and the private gate stay human (autonomy attribution) |
| 06 `map` | README + `run.py` that prints the ladder with our steps placed | The five real systems and which file they change; regular vs loop vs graph vs RSI as one table (`diff -r` between steps); the terminology table; the acceptance test; every external number marked *reported* with its source. | reading |

Seven steps, five packs. "Regular vs RSI" is `diff -r step_00 step_05`: memory,
traces, a verifier pack and a meta pack — not a new model, not a new slogan.

Data sources (for the README and `data/README.md`): OpenML 1590 (canonical id
for the skill), UCI Adult (original `adult.data` + `adult.test`, CC BY 4.0),
Hugging Face `scikit-learn/adult-census-income` (CSV alternative). The recipe
fields are `scale_numeric`, `encode_categorical`, `model`, one of
`C` / `max_depth` / `learning_rate`, `class_weight`; the 24-recipe grid over
logreg | rf | hgb from the owner's plan is the static list in every pack.

## What each test proves (offline, fake model, no key)

- 00: the fake model follows `SKILL.md` (24 fits in schema order, best-val pick, one `score_test`); the 25th `fit_recipe` returns `Error: budget exhausted` and the second `score_test` returns `Error: test already scored`; an unknown / disallowed / malformed tool call is an `Error:` result and the loop continues; the log is append-only; the same pack twice gives the same log.
- 01: `loop.json` is honoured by the tools (the 25th fit and a pre-FREEZE `score_test` are `Error:` results); the audit log is never read back; the pack is byte-identical after a run.
- 02: an illegal path is skipped and counted as an error, never replaced; `score_test` is unreachable before FREEZE; `graph.json` is byte-identical after a run.
- 03: the verifier pack boots with only the log (its transcript contains no actor text — asserted); `write_card` refuses a card mentioning `test` or intent; a planted wrong card is demoted after k counterexamples; with the rule-driven fake model, same seeds and budget, the memory arm's mean val AUC ≥ the `MEMORY_OFF` arm and its wasted fits are fewer; `MEMORY_OFF` reproduces step 00's numbers exactly.
- 04: test scored exactly once; all scorecard fields present; on the shifted table the frozen pack beats `MEMORY_OFF` on ≥ 3 of 5 seeds and the report names a card that did not transfer.
- 05: `rank_policies` makes zero fits (the budget counter proves it); a policy preferring unvisited recipes scores "unknown", never a number; broad-then-deep wastes fewer fits on a solved family than uniform (counted).
- 05 (continued): generation n+1 boots the files generation n wrote (checksums in the trace); a patch that raises val and lowers `private_score` is rejected and `versions/` restores the previous pack; `META_OFF` leaves the pack byte-identical; the meta pack cannot call `score_test` (not in its `tools.md` → `Error:`).
- All: `python run_tests.py rsi` and `python check_snippets.py rsi` green.

## Validation of the source tutorial against the papers (18 Sep 2026)

Repos and abstracts checked: RSIAgent (arXiv:2609.15364, AetherLabsAI/RSIAgent), Dream-RSI (2609.14858, code "being prepared"), ModularRSI (2609.14857; modules are Agent Loop, Tool Use, Observation Management, Context Management, Task Completion Detection — not "verification"), ScienceBuddy (2609.17523, inner harness loop + outer RL loop), Recuris (2608.24876, +32.2 on the longest tasks), the framework paper (2609.11873) and the July corpus survey (2607.07663). The tutorial's descriptions match; its specific numbers (162×, 71.97→78.98, 47.57→52.43, 8×A100, gpt-6-astra as improver) are not in the abstracts and stay *reported*. Conceptual corrections the series makes: "bounded vs strong" is too coarse (use the rungs + structural/effective); the locked test split is the wrong hero test (transfer table); free-text cards are not executable (typed cards + tool enforcement); a verifier that sees only `{recipe, val_auc, error}` cannot write the cards shown (it also gets the profile); single-seed "delete the file" claims are noise (paired seeds, matched budget); "replay each policy on the log" is under-specified (rank logged recipes, unvisited = unknown); no rollback anywhere (versions + private gate).

## Repo wiring

`rsi/README.md` (series codelab, top-down); `run_tests.py` / `check_snippets.py` already know `rsi/step_*`; `rsi/data/` bundled sample + licence note; `rsi/common/` harness, tools, fake model, loader, synth; `scikit-learn`, `pandas`, `numpy` in `requirements.txt`.
