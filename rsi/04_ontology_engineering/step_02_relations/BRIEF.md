# Lab 04.02 brief

A readable relation table describing one ML experiment.

Starting state: The vocabulary and baseline artifacts. Use a fresh Markdown facts file.

Prediction to ask: Does “model measured by MAE” tell you which action runs next?

Execution limit: No fits; inspect one correct relation table.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: Each relation is understandable as a sentence. The checker runs and the report states its limited coverage.
