# Lab 08.02 brief

A selected recipe evaluated once on the final public partition, with further search closed.

Starting state: A completed selection ledger and one preselected candidate. Use its original workspace.

Prediction to ask: What should happen if you request a new candidate after seeing the final score?

Execution limit: One final evaluation; no subsequent selection in that workspace.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: Final evaluation uses the recorded recipe and original training rows. A later fit request is rejected. The report states that the source is public.
