# Lab 10.27 brief

Three stores with different retention rules, including a rejected skill edit whose lesson survives.

Starting state: One failed task-skill proposal, raw trace, and current accepted skill.

Prediction to ask: Can a failed proposal add useful knowledge without becoming the active procedure?

Execution limit: Two fixture checks; no new fit required.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The rejected skill is not active. The notebook retains evidence-linked learning. Role access is labelled as an instruction unless technically enforced.
