# Lab 10.29 brief

A local results page, one interaction trace, and a revised inspection skill.

Starting state: Existing experiment results. The agent generates a static local page with a candidate table and one filter.

Prediction to ask: Could a critic mark a task complete even when the visible page shows the wrong candidate?

Execution limit: Two short UI attempts; no new ML fits. Requires a browser-capable agent for live execution.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The page uses actual data. Live interaction is distinguished from reading HTML or a simulation. Missing browser capability is reported rather than claiming completion.
