# Lab 10.28 brief

A small procedure graph with one tested transition edit and retained rejected proposals.

Starting state: The ML workflow graph, success and failure traces, and domain ontology.

Prediction to ask: Can changing a transition fix a workflow without changing any domain definition?

Execution limit: One graph edit and four fixture checks: two parent/child selection pairs, one frozen distinct case, and one semantic-error case. Six graph traversals total. Use a fit stub; no training.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The edited graph actually runs. Its test result is not replaced by the best intermediate selection score. Ontology and procedure remain separate.
