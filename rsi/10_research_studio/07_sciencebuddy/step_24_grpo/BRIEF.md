# Lab 10.24 brief

A tiny numerical grouped-reward update illustration with an explicit limit statement.

Starting state: Four synthetic rollout rewards: 0, 0, 1, 1. A small generated calculation; no pretrained model.

Prediction to ask: What happens when all four rewards are identical?

Execution limit: One CPU numerical calculation and two edge cases; no LLM training.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The calculation is reproducible and handles zero variance. No LLM checkpoint is claimed. The report lists missing pieces of real training.
