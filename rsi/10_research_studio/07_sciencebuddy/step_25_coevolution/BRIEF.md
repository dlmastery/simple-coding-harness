# Lab 10.25 brief

A version table and a small labelled simulation of alternating model and harness changes.

Starting state: Two synthetic model versions and two synthetic harness versions, plus a declared outcome table.

Prediction to ask: Must the best harness for model A remain best for model B?

Execution limit: Four table evaluations in a simulation; no LLM training.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: Every simulated result names both versions. Synthetic values are never mixed with paper or classroom measurements. The fixed reflector and feedback-source distinctions are checked against the paper.
