# Lab 10.34 brief

An interface-mismatch experiment and a source audit of local versus whole-trajectory correction.

Starting state: A small harness expecting a fixed tool-result format and two labelled response fixtures.

Prediction to ask: Can a more polished response fail because it violates a tool contract?

Execution limit: Four interface checks: valid, mismatched, locally repaired, and incompatible-template counterexample. No LLM training.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The parser failure and repair are executed. The source audit distinguishes parameter training from an interface demonstration.
