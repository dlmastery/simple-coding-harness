# Lab 10.06 brief

Two memory stores with different lifetimes and update rules.

Starting state: A completed ML trace and a partially completed new run.

Prediction to ask: Should the next task inherit the previous task’s “one attempt remaining” note?

Execution limit: No fits. Two retrieval checks.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The new state reflects the new task. Experience retains its evidence and scope. Stale identities are rejected.
