# Lab 10.04 brief

An outcome check and a separate actor-authored memory update.

Starting state: The exploration traces and a valid output checker.

Prediction to ask: Can a correct outcome verdict be followed by an incorrect lesson?

Execution limit: No fits. Verify one result and test one memory statement.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The verifier checks the result; the actor writes memory. Memory quality is inspected separately. No invented approval is recorded.
