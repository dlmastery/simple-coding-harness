# Lab 10.10 brief

A failure diagnosis and one restricted edit to a harness component.

Starting state: One successful and one failed ML workflow trace under the same task contract. If no suitable pair exists, prepare two labelled handoff fixtures with a fit stub first.

Prediction to ask: If the tool output is correct but the agent misreads its units, which component deserves attention first?

Execution limit: One component edit and two patch checks; at most two fits if real fitting is needed. Preparing a missing trace pair permits two separate prerequisite fixture captures with zero fits; record them in the ledger.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The edit stays within its declared component. The retained outcome includes both target and regression checks.
