# Lab 10.11 brief

An integration check for two individually tested harness edits.

Starting state: Two versioned component edits and their individual tests. If only one edit exists, prepare a second small interface change before declaring the comparison. Use executable fixtures with a fit stub; no model training is needed.

Prediction to ask: Can two individually passing components fail together?

Execution limit: Seven fixture executions: four versions on the original case, baseline and combined versions on one fresh case, and one malformed-interface check. No fits.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The combined version has its own tests and identity. Transfer cases are distinguished from selection cases. Failed integration is not hidden. Seven fixture records account for the complete budget.
