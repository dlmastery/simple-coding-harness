# Lab 10.11 brief

An integration check for two individually tested harness edits.

Starting state: Two versioned component edits, their individual tests, and a fresh transfer fixture.

Prediction to ask: Can two individually passing components fail together?

Execution limit: Three fixture runs; at most three fits if needed.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The combined version has its own tests and identity. Transfer cases are distinguished from selection cases. Failed integration is not hidden.
