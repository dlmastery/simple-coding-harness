# Lab 03.03 brief

A join that waits for both a data-quality check and a resource check.

Starting state: The branch router and a candidate recipe. Use small local check fixtures.

Prediction to ask: Should a data pass for candidate A combine with a budget pass for candidate B?

Execution limit: No model fits; three join scenarios.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The join rejects mismatched identities and incomplete evidence. The report states whether checks ran sequentially or concurrently.
