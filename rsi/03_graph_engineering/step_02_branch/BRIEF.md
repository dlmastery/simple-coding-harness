# Lab 03.02 brief

A branch that sends invalid data to repair and valid data to modeling.

Starting state: The data report and dependency graph. Make teaching copies; preserve pinned data.

Prediction to ask: What should happen when a data check has no result at all?

Execution limit: No fits required. Test two data fixtures and one ambiguous case.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: All three states are exercised. No invalid or unknown input reaches the fit action. Fixture mutations stay outside source data.
