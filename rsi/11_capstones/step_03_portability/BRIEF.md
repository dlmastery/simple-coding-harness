# Lab 11.03 brief

A compatibility matrix backed by actual small runs and clearly marked untested paths.

Starting state: Your frozen harness, a second task or agent environment, and optional available larger-compute backend.

Prediction to ask: Does a successful Codex run prove the same hooks work in Claude or Gemini?

Execution limit: Two small smoke runs; larger jobs require a concrete resource plan.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: Every support claim corresponds to a run. Task transfer, agent compatibility, and backend compatibility are not merged into one label.
