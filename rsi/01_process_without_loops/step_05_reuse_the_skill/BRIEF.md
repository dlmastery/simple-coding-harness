# Lab 01.05 brief

A handoff that another session can execute from files alone.

Starting state: The learner-owned baseline skill, task brief, and pinned repository. Start a genuinely new agent session if your host supports it.

Prediction to ask: Which information must survive outside the old conversation for the run to succeed?

Execution limit: One baseline fit in a new workspace.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The handoff identifies every required artifact. The new run is real. The report distinguishes fixed reuse from adaptive improvement.
