# Lab 01.04 brief

An output checker that derives error from prediction rows and rejects a mismatch.

Starting state: The fixed baseline skill and a successful prediction file.

Prediction to ask: Could a mathematically correct checker still approve the wrong candidate?

Execution limit: No model fits; two checker runs.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The valid file passes and the substituted-row file fails. CHECK-REPORT.md describes exactly what the checker reads and cannot protect.
