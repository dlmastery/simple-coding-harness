# Lab 05.01 brief

A fixed research workflow that uses a task skill, an ML tool, domain checks, and a report checker.

Starting state: The baseline skill, workflow graph, domain rules, and output checker.

Prediction to ask: Which component should stop a leaked feature before fitting?

Execution limit: One bike baseline fit and the existing checks.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The valid run passes the intended checks. The invalid fixture does not reach fitting. No component is silently revised during the comparison.
