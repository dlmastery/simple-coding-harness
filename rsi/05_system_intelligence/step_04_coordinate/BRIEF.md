# Lab 05.04 brief

A coordinator that advances work only when the required artifacts and checks are present.

Starting state: The fixed workflow, context packet, and matching candidate checks.

Prediction to ask: Should a coordinator repair an ambiguous target definition automatically?

Execution limit: One baseline fit; two simulated handoff failures.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The coordinator never marks incomplete work complete. Its report states that role separation is organizational unless an actual access boundary exists.
