# Lab 02.01 brief

A controlled comparison between a constant baseline and a calendar-based linear model.

Starting state: The bike data card and the constant baseline’s hour-by-hour errors. Use a fresh comparison workspace.

Prediction to ask: Will the linear model reduce the selection MAE? What result would contradict your explanation?

Execution limit: Two fits: constant and linear, both with calendar features and seed 17.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The data, split, metric, seed, and feature set match. Only the model family changes. COMPARISON.md retains both results and costs.
