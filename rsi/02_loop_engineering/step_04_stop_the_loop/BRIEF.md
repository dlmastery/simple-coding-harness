# Lab 02.04 brief

A loop controller that stops on a repeated recipe, exhausted budget, or unchanged failure.

Starting state: The three-attempt loop and recorded candidate recipes.

Prediction to ask: Should the sequence linear → tree → linear count as three new ideas?

Execution limit: At most two new fits; duplicate proposals must be detected before fitting.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The controller produces a real refusal for both duplicate and over-budget requests. Rejections are recorded without fabricated model scores.
