# Lab 09.05 brief

A matched comparison of improvements produced by two improver versions.

Starting state: Frozen v0 and v1 improvers, identical parent task skill, and prespecified fresh comparison cases.

Prediction to ask: Could v1 win on the current task but lose on improvement produced per unit cost?

Execution limit: Two rounds per improver, at most two fits per round; eight fits total. Count proposal and check overhead.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: Both arms start from the same solver. Their resource limits and known costs are reported. The conclusion concerns the tested improvers and tasks only.
