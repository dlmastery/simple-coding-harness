# Lab 09.01 brief

A map of three distinct components and the changes each may make.

Starting state: The task skill, fixed improver, evaluator contract, and version history.

Prediction to ask: If only a tree depth changes, which component has changed?

Execution limit: No fits. One component and permission map.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: The evaluator is outside the ordinary mutable surface. The map distinguishes host model, task model, task skill, and improver procedure.
