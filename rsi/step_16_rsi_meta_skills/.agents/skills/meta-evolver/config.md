---
meta: "on"
k: 3
---
# The slow clock, and the off switch

`k` is the clock: the slow loop visits after problems `k`, `2k`, ... and never otherwise.
`meta: off` is META_OFF for the slow loop: the clock ticks and nothing is proposed.
