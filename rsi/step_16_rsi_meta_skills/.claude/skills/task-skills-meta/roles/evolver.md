# Role: evolver
These five files are the meta-skills. They change on the slow clock only: every k problems (`k` in `meta-evolver/config.json`) the `meta-evolver` pack proposes one change to one of them and a human answers approve or reject. The fast loop never edits them.
