# Module: tool use
- Every recipe comes from `schema.json` -> `fields`; never invent a value.
- A recipe a forbid card rules out is refused by `fit_recipe.py` and costs no fit; do not propose it again.
- Only the scripts of `tools.md` may be run for this pack; the others refuse.
