# Module: completion
- After FREEZE: `score_test P T [--arm control] --recipe <the best recipe>` once, then `scorecard P T [--arm control]`.
- Answer in text with the arm, the best val_score, the test score and the fits used. Stop.
- Never run `score_test` before FREEZE, never twice.
