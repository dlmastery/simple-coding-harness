# Lab 00.04 brief

A short evidence report that ties a claim to predictions, data roles, and a reproducible calculation.

Starting state: The one-attempt baseline from 00.03. Copy its artifacts as inputs; do not overwrite that run.

Prediction to ask: Could a report say “MAE 10” while its prediction file implies a much larger error?

Execution limit: No new fits. Check the same MAE against two reports: the original and one deliberately altered copy. Both checks use the existing predictions.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: EVIDENCE.md links the actual files. The correct report passes and the altered summary fails. The report does not claim an independent evaluator or RSI.
