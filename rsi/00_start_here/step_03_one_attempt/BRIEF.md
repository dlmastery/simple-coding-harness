# Lab 00.03 brief

A real constant-prediction baseline with a measured error and saved predictions.

Starting state: A fresh workspace, the verified bike data, and the fixed task brief.

Prediction to ask: Will one fixed count handle both quiet nights and busy afternoons well?

Execution limit: One constant-model fit. No search and no final evaluation.

Follow the README steps. Keep source data and the supplied evaluation contract unchanged. Use the canonical course skills. Generate any required code yourself. Save observations, failures, and the learner’s progress in the separate workspace. Do not invent student answers, measurements, or protected evaluator access.

Acceptance: There is exactly one successful trial. The prediction is constant. The reported full MAE matches a recomputation over saved predictions. No final evaluation has run.
