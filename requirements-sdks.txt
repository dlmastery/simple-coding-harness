# Steps 15-18: the same harness on four agent SDKs.
# Install on top of requirements.txt. Each step's tests skip if its SDK is absent.
claude-agent-sdk>=0.2
openai-agents>=0.22
google-antigravity>=0.1.16
deepseek-harness-sdk>=0.1.5rc1   # pre-release; pip needs the rc pinned to pick it up
