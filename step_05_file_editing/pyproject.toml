[project]
name = "coding-agent"
version = "0.4.0"
description = "A minimal coding agent, stage 4"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.50",
    "pyyaml>=6.0",
    "rich>=13.0",
]
